# 更新日志 · CHANGELOG

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> ⚠️ **版本体系说明**：本文件的版本号是**交付版本**（对外）；
> 《架构设计》文档内的 `v1.2` 是**文档版本**（对内），两者属不同体系，勿混用。

---

## [0.1.0] · 2026-09-16

首个可交付版本。从「一次性脚本」升级为 **PySide6 桌面应用 + 单 exe 免安装交付**。

### 新增（功能）

- **四层架构落地**：`ui (PySide6) → app (编排) → core (判定引擎) → infra (基础设施)`，
  依赖方向单向，由 `tests/test_architecture_guard.py` 机器强制断言
  （`core/` 与 `app/` 禁止 import PySide6）。
- **Excel 结构变体探查 A–E**：覆盖首行表头 / 制单 Sheet（有无冒号）/ 双 Sheet 陷阱 /
  无订单号列；含 P4 双通道列映射自校正。
- **图片三级索引匹配**：`{票号}\{料号}[-后缀]\{料号}&{订单号}&{序号}.jpg`，
  料号目录模糊匹配 + 子目录兜底扫描 + 同料号多订单二次筛选。
- **OCR 引擎**：RapidOCR（`rapidocr` 3.9.2 + onnxruntime 1.30.0）。
  模型随 wheel 落盘（`rapidocr/models/`，约 31 MB），**离线可用**。
- **四类判定引擎**：✅ 校验合格 / ❌ 校验异常 / ⚠️ 缺图内标识，人工复核 / 🔵 缺图，人工复核。
  判定字符串与 SOP 1.3 **逐字一致**，由 `tests/test_constants.py` 硬断言锁定。
- **噪声三态护栏（NoiseGuard）**：落实「不虚高」红线——疑似 OCR 噪声不直接上报为异常。
- **规则外置**：`rules/` 下 7 个 YAML，**补规则 = 改配置不改代码**。
  启动时一次性加载为不可变快照（不做热加载，防跑批中口径漂移）；
  现场可用 `%APPDATA%\CustomsChecker\rules\` 覆盖内置规则。
- **三产物导出**：`校验汇总表_{票号}.xlsx`（恰好 13 列）+ `{票号}_待人工复核清单.csv`
  （utf-8-sig）+ `校验详细日志_{票号}_累积.json`（含完整 OCR 原文）。
- **断点续跑（C4）**：断点与数据源指纹绑定，防串票；
  UI 显式呈现「重新开始 / 继续执行」双态。
- **共享盘全 UNC 支持（C5）**：Excel 与图片目录均支持 UNC；不可达时明确报错不崩溃。
- **只读红线守卫**：`infra/fs_lock.py` 的 `input_readonly / assert_readonly / assert_within`
  在写入前断言目标归属，越界抛 `OutputPathViolation`。
- **环境体检闸门**：`tools/check_env.py` 检测 pip 空壳包/残缺包
  （19 个关键依赖 + 23 项模块 import），被 `build_exe.ps1` 第一步强制调用。

### 修复（真 OCR 跑批暴露的 6 个缺陷）

首次真跑批 SA26090215（18 条）暴露 `PASS = 0` 的严重虚高，逐一定位并修复：

| 缺陷 | 现象 | 修法 |
|---|---|---|
| **A** | `Customer model` 被误列为整机上下文 token，几乎每张唛头图都命中 → 整机判定频繁误触发 | 从 `context_tokens` 移出（YAML + `constants.py` 同步） |
| **B** | `context_window: 3` 按行距检索；实测唛头上 `Brand:` 与 `CARTON No.` 垂直相距 7–8 行 → **同构图结果自相矛盾、不可复现** | 改为 `context_scope: whole_image` 整图全文检索 |
| **C** | 品牌提取「首图首命中」，取到 `ONEL`/`prime`/`AAA` 等噪声 token（真品牌在第 9 张图才出现） | 改为**跨图一致性投票** + 标签图 `&001` 加权；分层 `min_images`（①②③ 层 = 1，④ 裸 token 层 = 2） |
| **D** | `无品` 不在 `none_tokens`，标签上「品牌:无品」被当有效品牌 | `无品` 加入 `none_tokens` |
| **E** | `_apply_blacklist` 的 `skip_prefixes` 误伤型号字段 | 改为「捕获词前 8 字符窗口」，且型号字段 `skip_prefixes` 置空 |
| **F** | `_resolve_noise_rules()` 兜底分支漏传 `known_noise_samples` 等 3 字段 → 打包漏 YAML 时静默失效 | 兜底分支补传 3 字段，**保证兜底 ≡ repo 路径规则集**；新增 2 把回归锁 |

> **缺陷 F 的根因经历三次诊断、两次纠正**（"加载链路断裂" → "粒度不匹配" → 最终
> 靠对照实验定位为"兜底分支漏传字段"）。演进链完整记录在
> `docs/05_架构裁决_真OCR误报缺陷_R4b.md` §5B.0，作为方法论案例保留。

### 打包与工程化加固（交付前加固）

| # | 项 | 说明 |
|---|---|---|
| 1 | **依赖声明补齐** | `requirements.txt` / `pyproject.toml` 原**漏声明 `openpyxl`** —— 而 `core/result_exporter.py`、`core/excel_probe.py` 强依赖它。干净环境装完仍 `ImportError`，打包后 exe 一导出汇总表即崩且用户机无 Python 无法补救。同时把 `flatbuffers` / `antlr4-python3-runtime` / `requests` / `certifi` / `charset-normalizer` / `six` / `tqdm` / `colorlog` / `colorama` 由「注释里的口头承诺」改为**真实声明** |
| 2 | **依赖声明一致性回归锁** | 新增 `tests/test_dependency_manifest.py`（6 用例）：静态扫描 `infra/core/app/ui` 的第三方 import ↔ 依赖清单双向核对；`requirements.txt` ↔ `pyproject.toml` 交叉一致；`check_env.CRITICAL_DEPS` ⊆ 清单。让「漏声明」变成红灯 |
| 3 | **依赖安装脚本** | 新增 `tools/install_deps.py`：把「装依赖 → 摘除非 headless opencv → 重装 headless 确权 → 环境体检」串成一条命令。`rapidocr` 元数据写死 `opencv_python>=4.5.1.48`，与 headless 版**同占 `cv2/` 目录**且 pip resolver 无法表达互斥 —— 漏做纠正会让 exe 混入 Qt DLL 与 PySide6 双 Qt 冲突 |
| 4 | **`check_env` 命名空间包误报修复** | 原判据只看「目录名 == 发行版名」，把 PEP 420 命名空间包 `google/`（实际由 `protobuf` 发行版拥有，`dist-info` 名为 `protobuf-*`）误判为残缺包 → **exit 1 直接拦住打包**。新增「由其它发行版 RECORD 声明拥有」判据（读 `*.dist-info/RECORD` 与 `*.egg-info/SOURCES.txt`）+ 3 把回归锁 |
| 5 | **onefile 日志落点修复** | `main.py` 原以 `Path(__file__).parent / "过程产出"` 兜底，在单文件 exe 下解析成 `%TEMP%\_MEIxxxxxx`（进程退出即删，日志直接丢失）。改用新增的 `infra.resources.app_base_dir()`（打包态 = exe 所在目录），与 `bundle_root()`（= `_MEIPASS`）职责分离 |
| 6 | **`-Onedir` 开关生效** | `build_exe.ps1 -Onedir` 原只设了 `CUSTOMS_ONEDIR` 环境变量，而 `customs_checker.spec` **从未读取**它 —— 加与不加都出单文件。spec 内补 `EXE/COLLECT` 双分支实现 |
| 7 | **版本号归位** | 交付版本号统一为 `0.1.0`（`main.py APP_VERSION` + `pyproject.toml`）；与《架构设计》文档版本 `v1.2` 明确区分 |

### 修复 · exe 冒烟实测暴露的 4 个打包态缺陷

单文件 exe 首次真正跑起来后，`--self-test` 与 `--check-env` 两条**现场排错主路径
全部失效**。四个缺陷逐一修复，各自补回归锁：

| # | 缺陷 | 现象 | 修法 |
|---|---|---|---|
| 1 | **`--self-test` 打印 ✅ 时崩** | `SELF-TEST: PASS` 与 `SELF-TEST: FAIL - UnicodeEncodeError: 'gbk' codec can't encode '\u2705'` **同时**出现，退出码 1 —— 断言全过却报失败，误导排错方向 | 原 `force_utf8_encoding()` 只设环境变量，而它对**已打开的** std 流无效（进程启动时已按简体中文 Windows 的 ANSI 代码页建好）。新增 `_reconfigure_std_streams()` 显式 `reconfigure(encoding="utf-8", errors="replace")`，并容忍 stdout 为 None / 无 reconfigure 的对象 |
| 2 | **`--check-env` 在 exe 内挂死** | 现场表现为"敲了命令没反应"：找不到 `tools/check_env.py` → `ImportError` → 窗口程序（`console=False`）弹**模态错误框** → 无人值守时永久挂起（实测前台 300 s 无输出） | 三级查找 `bundle_root()/tools` → `app_base_dir()/tools` → `__file__` 同级；找不到时打印可读原因（含全部尝试路径）并返回 1，**绝不抛异常**；同时把 `tools/check_env.py` 打进 spec 的 `datas` |
| 3 | **`--check-env` 误报 13 个"残缺包"** | 把 exe 自己的 `_MEIPASS` 扫成残缺包（`PySide6`/`numpy`/`PIL`/`rules`/`ui` …）并 exit 1 | PyInstaller 把纯 Python 模块编译进 **PYZ 归档**，`_MEIPASS` 只落地二进制与数据文件 → 「有文件但无 `__init__.py`」判据在打包态**根本不成立**。新增 `is_frozen()`，打包态下该节改为**明确声明「不适用」**（不再误报）；报告头加「运行形态」行 |
| 4 | **`find_site_packages()` 摸到宿主机** | onefile 把依赖平铺解到 `sys._MEIPASS`（目录名不是 `site-packages`），原实现顺着 `sys.path` 一路找到**宿主环境**的目录 | 优先识别 `sys._MEIPASS`（存在且为目录时直接返回） |

**连带发现（由 exe 自检抓出）**：`six` **没有被烤进 exe** ——
PyInstaller 只按「静态可解析的 import」收集模块，而 `six` 在任何代码里都没有
静态 import 点，于是静默漏收。已把 `six` / `colorlog` / `colorama` / `antlr4` /
`flatbuffers` / `tqdm` 一并加入 spec 的 `collect_submodules` 清单，
并新增 `test_spec_explicitly_collects_transitive_only_deps` 锁定（以后新增此类
「传递依赖」若忘了加，测试直接红灯）。

> **方法论记录**：这 4 个缺陷**全部只在打成 exe 之后才暴露**，源码态 629 用例
> 全绿时一个都看不出。说明「源码测试通过」≠「交付物可用」——
> **`--self-test` / `--check-env` 这类 exe 自检不是锦上添花，是交付前必须跑的一道关**。

### 明确不改（口径守住）

两项曾被提出、**经复审后撤销**的改动，理由均援引 SOP 原文：

| 议题 | 结论 | 依据 |
|---|---|---|
| 整机品牌命中是否改判 ⚠️ | **不改**，保留 ❌ | SOP 3.4 第 147 行「字段缺失 → ❌」+ 附录 A.4「13 合格 / 5 异常（全为外箱整机品牌类）」 |
| OCR 已知误读如何处置 | **自动纠正后比对**（`SKYHORTH` → `SKYWORTH P/N`），非独立判噪 | 减少虚高，同时不放弃可比对证据 |

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **656 用例 / 0 失败 / 0 错误**（`pytest -q`，RC=0） |
| 静态检查 | `ruff` All checks passed |
| 环境体检（源码态） | `tools/check_env.py` exit 0（19 依赖 + 23 import + 无空壳包） |
| 真 OCR 端到端 | 113 张图 / 535.9 s（平均 4.74 s/张），四层硬断言 ALL PASS |
| 离线回放一致性 | 与真 OCR 链路**四类分布完全一致** |
| 原始输入只读 | 130 文件 / 394,140,543 字节，跑批前后 (文件名+size) sha256 一致 |
| 修复前后分布 | ✅ 0→1 ／ ❌ 11→12 ／ ⚠️ 7→5 ／ 🔵 0→0 |
| **exe 产物** | `dist/报关申报要素校验工具.exe`（149 MB，单文件，不入库） |
| **exe `--self-test`** | exit 0，规则加载 / 13 列 / 口径字符串全部通过，✅ 正常输出 |
| **exe `--check-env`** | 19 依赖版本核对 + 23 项模块 import 全过（空壳包扫描按打包态声明不适用） |


### 已知限制

1. **单文件 exe 需在干净 Win10/Win11 x64 机器上做最终验收**（C1）——
   本机已产出 exe 并通过自检，但「双击即运行」的现场验收仍待执行；
   `dist/` 与 `build/` 不入库。
2. **`seq=5` 归属待裁决**：`rules/non_brand_tokens.yaml` 的排除词表目前只对
   ④「裸 token 层」生效，③「品牌+型号行层」无 `non_brand` 过滤 →
   PCB 丝印残片（实测 `NMY` / `EMV`）可穿透被当作品牌。
   属**规格边界问题**，按「口径变更需确认」护栏**未擅自改动**。
3. 真 OCR 与离线回放在 `seq 10–18` 有 9 条**品牌取值**差异（OCR 行序与置信度所致），
   四类分布一致，但差异本身未逐条归因。

---

## 未发布

### ✅ 已裁定并执行 · 交付包构成（手册随包出厂）· 2026-09-17

**发现**：打包后复核 `dist\` 时发现**同版本存在两个 zip 且内容不同**，逐文件取证如下——

| 文件 | 大小 | 条目 | 含操作手册 |
|---|---|---|---|
| `dist\报关申报要素校验工具_v0.3.7_0917.zip` ← `build_exe.ps1` 声明的交付物 | 157,672,183 B | 731 | ❌ **不含** |
| `dist\…_v0.3.7_0917\报关申报要素校验工具.zip` ← 藏在版本目录内 | 158,190,638 B | 828 | ✅ 含（+3 条目） |

**等价性核对结论**：两个 zip 的 **app 本体逐文件逐字节完全一致**（只在交付包 0 个 / 只在内层 3 个、且 3 个全是手册 / 同名不同大小 0 个）
→ **exe 功能无任何差异**，差别**仅在于是否附带 `操作手册.html` + `操作手册插图\`**。

**时间线**（取证于 `过程产出\v0.3.7\v0.3.7_时间线取证.txt`）：

```
11:43:55  ① build_exe.ps1 -Zip 产出交付名 zip（Compress-Archive 只压 app 目录 → 不含手册）
13:59:00  ② 面向一线员工的操作手册 v0.3.7 完成，并放入 onedir
14:03:12  ③ 重新压缩 onedir → 落到版本目录内的「报关申报要素校验工具.zip」（含手册）
          ✗ 该含手册版本**从未被提升为交付名** → 交付名 zip 与 onedir 内容不一致（遗留）
```

**根因**：`build_exe.ps1` STEP 6 压缩的是 `dist\报关申报要素校验工具`，**设计上本就不含手册**；
第 ③ 步「手工塞进 onedir + 重压」属**绕开脚本的临时动作** → 每次打包都会复现不一致。

**用户裁定（2026-09-17）**：**手册随包出厂**。

**已执行修复（根因，非补丁）**：

| # | 落点 | 变更 |
|---|---|---|
| 1 | `build_exe.ps1` STEP 6 | 新增 **6a 手册入包**：压缩**之前**把 `成果产出\操作手册_v<版本>_<日期>.html` 复制为 app 目录下 `操作手册.html`，并把 `操作手册插图\` 整目录复制入包；**找不到手册即 `exit 1`** |
| 2 | `build_exe.ps1` param | 新增 `-SkipManual`（显式排除）与 `-ManualHtml`（自定义手册源）；手册源路径由 `$SpaceRoot = Split-Path -Parent $Root` 推导，**必须在 `$Root` 赋值之后计算** |
| 3 | `build_exe.ps1` 尾部提示 | 交付物说明补「包内附一线员工操作手册」 |
| 4 | `dist\` 形态统一 | 迁移 `dist\…_v0.3.7_0917\`（旧暂存布局）入 `归档\dist\`，`dist\` 只保留**一个**交付 zip + onedir |

**重新打包并复核**（`过程产出\v0.3.7\v0.3.7_二次打包日志.txt` / `…_二次打包后取证.txt`）：

| 项 | 结果 |
|---|---|
| 打包 | `build_exe.ps1 -Zip` rc=0，耗时 **97.9s**；日志含 `手册入包：操作手册.html (365.3 KB)` + `插图入包：操作手册插图\ (2 个文件)` |
| 交付 zip | **158,180,210 B（150.9 MB）**，sha256 `dc92dda23f00849f82150c27d2d5eac48eb74d0dcd4897079221634dfbe8f375` |
| zip 结构 | 734 条目（724 文件 + 10 目录）/ 顶层 **1 个目录** `报关申报要素校验工具` / 根内 exe 就位 / `testzip` **OK** |
| ⭐ 手册入包 | **3 条目全部就位**：`操作手册.html` 374,018 B + `操作手册插图\01` 74,898 B + `02` 173,449 B |
| onedir | 724 文件 / 362,082,172 B；exe 10,396,766 B |
| rules | **8/8 sha256 与源码逐个相同** |
| 冻结态核对 | `tools/smoke_frozen.py` **六项全绿 rc=0** →「全部关键项通过，产物可交付」 |
| 只读红线 | `原始输入` **130 文件 / 394,140,543 字节** —— 与基线逐字节一致 |

**副产结论（勘误自身判据）**：初次复核时我按「zip **根下**应有 exe」下断言 → 误报 `❌ 根下无 exe`。
`Compress-Archive -Path <dir>` 会把**目录名本身**作为顶层条目，故结构为
`报关申报要素校验工具\报关申报要素校验工具.exe`（**标准 onedir 包装，本就正常**）。
`tools/smoke_frozen.py` 的「根下 exe」判据是按**顶层目录内**计算的 —— **脚本一直是对的，错的是我的临时判据**。
→ 固化教训：**核对包/目录结构先打印真实层级，再写断言**；差异必须逐文件比对到「同名不同大小 = 0」才算解释清楚。

### ✅ 已裁定并执行 · 归档

| 项 | 处置 |
|---|---|
| `dist\报关申报要素校验工具_v0.3.6_0917.zip`（157,656,652 B） | → `归档\dist\` |
| `成果产出\操作手册_v0.3.6_0917.html`（97,154 B） | → `归档\成果产出\` |
| `dist\报关申报要素校验工具_v0.3.7_0917\`（旧暂存布局，725 文件 / 520,257,474 B） | → `归档\dist\` |

- 执行留痕：`过程产出\v0.3.7\v0.3.7_归档执行.txt`（每项移动的源/目标/字节一致校验）
- 索引更新：`归档\README_归档索引.md` —— 新增「§6 归档历史」，归档总量 **1,461 → 2,189 文件 / 1,668,889,174 → 2,346,905,854 字节**；当前版本改为 v0.3.7
- `dist\` 现状：**仅 1 个 zip + 1 个 onedir**（合计 520,262,382 B），现场不会再装错版本

### 待办（未处理，原样保留）

- **待办 10 · UI-1~UI-4 续跑 UI 接线缺陷**（`resume_run_clicked` 无发射点等 4 条）：与 v0.3.7 四项需求无交集，**未动**。
- **建议（v0.3.8 候选，未实施）**：一线员工手册 §2 可加一句「操作手册.html 就在同一个文件夹里」——
  手册现已随包出厂，但手册正文尚未告知用户它自身在包内。改它需**重新生成手册 + 重新打包**，故留作候选。

## [0.3.7] · 2026-09-17（复核工作台四项现场优化：切换条下沉 / 命中标红 / 右上红星 / 人工重判标识与复核后版本）

> **触发（用户指令，原文）**：
> 「1、人工复核工作台中，图片的切换按钮建议放在图片下面，避免因为判定结果挤压导致看不到切换按钮，
> 同时增加左右切换的按钮，实现图片切换
> 2、对于能够命中判定结果的图片，其切换按钮用红色按钮，表示此图片命中了申报要素，其余的按钮为主题色即可
> 3、对于命中判定结果的图片，显示的时候右上角增加一个红色的五角星，表示此图片命中了结果
> 4、对于人工重判的记录，需要在结果表中进行标识（这条记录是人工重判的），同时重判结果不覆盖原系统产生的结果，
> 生成一个复核后的版本，且保存复核即落盘」

**性质判定**：**UI 层交互优化 ＋ 导出层「另存复核后版本」变更**。
**判定口径、判定链路（`core/judge_engine.py`）、四类判定字符串、13 列汇总表结构 —— 全部零改动。**

### 需求 ↔ 实现对照

| # | 需求 | 落点 | 做法 |
|---|---|---|---|
| 1 | 切换按钮放到**图片下面** + 增加**左右切换** | `ui/widgets/image_viewer.py` | 把证据图切换条从 `WorkbenchCard` **整卡纵向滚动区**里迁出，改为常驻在**图片正下方**（查看器自身布局，图片区拉伸 1、切换条自然高）；新增 `◀ 上一张` / `下一张 ▶` + `step_image(±1)` |
| 2 | **命中图**按钮用红色，其余主题色 | 同上 | 新增纯函数 `_switch_button_qss(*, hit, active)` 四态配色（命中红 `#D32F2F` / 未命中主题蓝 `#1565C0`）；命中集由工作台经 `set_images()` 注入 |
| 3 | **命中图**右上角红色五角星 | 同上 | 新增 `_ImageStage(QLabel)`，在 `paintEvent` 里画半透明白圆底 + 红色 `★`；**只在真的显示了图片时**才画 |
| 4 | 人工重判记录**在结果表标识** + **不覆盖原系统结果** + **生成复核后版本** + **保存即落盘** | `core/models.py` / `core/constants.py` / `core/result_exporter.py` / `app/review_store.py` / `ui/main_window.py` | 记录表新增**「复核」列**；`CheckResult.original_verdict` 留痕系统判定；`ReviewStore.persist()` 一次落「留痕 + 复核后版本」，**原三产物一字不动** |

### 需求 1 的根因（为什么"看不到切换按钮"）

原缺陷**不是按钮太小或颜色太淡**，而是**布局位置错**：

```
WorkbenchCard                                        ← 整卡纵向滚动
└── QScrollArea  widgetResizable(True)
    └── content
        ├── 三列判定链路（判定原因可长达数行）
        ├── 散行 OCR 文本（可长达十几行）      ← 高度不可控
        └── evidence_buttons_row  [图1][图2]…   ← 被推到折叠线以下 ✗
```

`WorkbenchCard` 自 v0.3.1 起的既定取向是「**宁可滚动，不可压扁**」（`_fit_content_height`
按 `heightForWidth` 给出内容最小高 → 触发纵向滚动条）。判定原因 + 散行 OCR 一长，
图号按钮就被推到**视口之外**，现场表现为"看不到切换按钮"。

**修法**：把切换条**下沉到图片查看器** —— 它与图片同处一个固定高度区域
（`outer` 布局里 `scroll` 拉伸 1、切换条自然高），**不受右侧卡片内容高度影响**。
`WorkbenchCard` 内的相关控件（`evidence_buttons_area` / `evidence_buttons_row` /
`_seq_buttons` / `_render_evidence_buttons` / `_style_seq_button`）**整段删除**，
并留注释说明迁出原因。

### 需求 4 与「13 列冻结红线」的冲突化解（关键设计）

13 列汇总表是**用户裁定冻结**的红线（`constants.COLUMNS` + `COLUMN_COUNT = 13`），
但需求 4 要求"结果表中标识人工重判"。两者并不矛盾，因为红线约束的是**导出产物**：

| 产物 | 复核标识 | 列数 | 是否被覆盖 |
|---|---|---|---|
| `校验汇总表_{票号}.xlsx` | ✗ 无 | **13（不变）** | 系统跑批时落盘，**复核后再也不动** |
| `{票号}_待人工复核清单.csv` | ✗ 无 | 原样 | 同上 |
| `校验详细日志_{票号}_累积.json` | ✗ 无 | 原样 | 同上 |
| **`校验汇总表_{票号}_复核后.xlsx`** | ✅ **「人工复核」列** | **13 + 2** | 每次保存复核即重写（另存文件） |
| **`{票号}_待人工复核清单_复核后.csv`** | ✅ 同上 | 原 8 + 2 | 同上 |
| **`{票号}_复核后.json`** | ✅ 每条带 `original_verdict` / `reviewed_at` | — | 同上 |
| **`复核留痕/review_round_N.json`** | ✅ 每条带 `original_verdict` | — | append-only，每保存一次一轮 |
| 工作台**记录表**（仅屏幕，非产物） | ✅ **「复核」列** | 4 列 | — |

- 复核后追加的**恰好两列**：`REVIEWED_EXTRA_COLUMNS = ["人工复核", "原系统判定"]`
  （列名与文案解耦：「人工复核」是**列名**，格内文案来自
  `CheckResult.manual_review_mark()` = `MANUAL_REVIEW_MARK` = `"人工重判"`）。
- 文件名中缀 `REVIEWED_NAME_SUFFIX = "_复核后"`，与原始产物**并列存在**，
  复核人可随时对照"系统判成什么 / 人工改成什么"。

**`original_verdict` 只在首次改判时落定**（`ReviewStore.set_verdict`）：

```python
original = getattr(result, "original_verdict", None)
if original is None:          # 只记一次
    original = result.verdict
    result.original_verdict = original
```

否则「原系统判定」会退化成"上一次的重判值" —— **可追溯红线**当场失效。
本版为该行为加了显式回归锁（`test_original_verdict_survives_second_rejudge`）。

### 「保存复核即落盘」

`ReviewStore.persist(exporter)` 一次调用落 **4 个文件**（顺序固定，任一步失败即上抛，
由 UI 明确提示而非静默）：

1. `复核留痕/review_round_N.json`（每保存一次即一轮，append-only）
2. `{票号}_复核后.json`（复核后完整结果集）
3. `校验汇总表_{票号}_复核后.xlsx`
4. `{票号}_待人工复核清单_复核后.csv`

接线：`MainWindow._on_verdict_applied` → `_persist_review()`（重判即落盘，状态栏提示
「已落盘复核后版本（原系统产物未改动）」）；菜单项与工作台按钮文案统一改为
「**保存留痕并导出复核后版本**」。`_on_export_reviewed` **不再调用 `export_all`** ——
不再覆盖原三产物。

### 命中判据（UI 侧只读，不重算判定）

新增模块级纯函数 `ui/widgets/review_workbench.py::hit_image_paths(result)`：

* `TokenMatch.hit`（`EXACT` / `FUZZY`）→ 其 `images` 图号对应的图 = "申报值以**完整分词**
  出现在该图中"；
* `TokenMatch.none_marker` 非空 → `none_marker_image` 对应的图 = "图内显式标注了
  『无品牌 / 无型号』"（口径 v0.3.2 下判 ✅ 的直接证据）；
* **图号找不到对应文件 → 忽略该图号**（不臆造路径）；
* `MATCH_NONE`（未命中）即便带图号也**不得**标红。

UI 只做 `seq → path` 映射，**不重算任何判定**（守住「判定口径唯一来源 = `core/constants.py`」）。

### 改动清单

| # | 位点 | 修法 |
|---|---|---|
| 1 | `ui/widgets/image_viewer.py` | 新增 `_switch_button_qss()`（纯函数，四态）、`_ImageStage(QLabel)`（`paintEvent` 画星，`_STAR_SIZE=28` / `_STAR_PAD=8`）、`Signal image_requested(str)`、`_build_switcher()`（`QFrame#imageSwitcher`）、`set_images` / `image_paths` / `hit_image_paths` / `set_current_image` / `current_image_path` / `step_image` / `_request_image` / `_sync_switcher` / `_clear_thumb_buttons`；`show_message` / `load` / `clear` 均加清星 + 同步切换条 |
| 2 | `ui/widgets/review_workbench.py` | 新增 `hit_image_paths()` / `_evidence_items()` / `_load_image()`（换图**唯一落地点**）/ `_on_image_requested()`；记录表 4 列（新增「复核」列 + `_TABLE_COLUMNS` + 最小宽 440）；`__all__` 导出 `hit_image_paths` |
| 3 | `ui/widgets/workbench_card.py` | **删除**切换条相关控件与方法（`evidence_buttons_area` / `evidence_buttons_row` / `_seq_buttons` / `_render_evidence_buttons` / `_style_seq_button` / `_SEQ_BTN_QSS`）；保留 `_on_seq_button()` 供 OCR 弹窗翻页复用 |
| 4 | `core/constants.py` | 新增 `MANUAL_REVIEW_MARK = "人工重判"`（**记录表列与导出列共用的单一出口**） |
| 5 | `core/models.py` | `CheckResult` 新增 `original_verdict: Verdict \| None` / `reviewed_at: str`；新增 `manual_review_mark()` / `original_verdict_text()`；`to_dict()` 追加 3 键（**不占 13 列**） |
| 6 | `core/result_exporter.py` | 新增 `REVIEWED_NAME_SUFFIX` / `REVIEWED_EXTRA_COLUMNS` / `summary_reviewed_path()` / `review_csv_reviewed_path()` / `_reviewed_extra()` / **`export_reviewed_all()`**；`export_summary` 与 `export_review_csv` 增 `reviewed: bool = False` 关键字参数；`export_all` docstring 明确其只产**系统原始版本** |
| 7 | `app/review_store.py` | `ReviewOverride` 新增 `original_verdict`（进 `to_dict`，`from_dict` 兼容旧文件）；`set_verdict` 首次改判记录系统判定 + 写 `reviewed_at`；**新增 `persist(exporter)`**（留痕 → 复核后 JSON → 复核后汇总表 → 复核后清单） |
| 8 | `ui/main_window.py` | `APP_VERSION = "0.3.7"`；新增 `_current_exporter()` / `_persist_review()`；`_on_verdict_applied` 重判即落盘；`_on_export_reviewed` 只产复核后版本；`_reset_runtime_views` 新增 `_review_store.reset()`（**修掉一个潜在缺陷**：原先未清 store，上一轮 override 会以「幽灵改判」套到新一轮结果上） |
| 9 | `ui/widgets/summary_panel.py` | `show_artifacts` 支持 `summary_reviewed` / `review_reviewed` / `reviewed_json` 三键，标题切换为「复核后版本已落盘（原系统产物未改动）：」 |
| 10 | `tools/ui_preview.py` | 新增 `--hit` / `--reviewed` 两个渲染模式（命中态 / 重判态验收图）+ `_materialize_images()`（把夹具图写成真 PNG，否则 `load()` 失败、**五角星不画**） |
| 11 | `tests/test_v037_workbench.py`（**新文件**） | v0.3.7 四项需求回归锁 **41 条**（含下方改动 13 / 14 两条缺陷的回归锁） |
| 12 | `tests/test_ui_workbench_d_class.py` | 迁移失效断言：`test_record_table_has_three_columns` → `_four_columns`（+「复核」列）；原 `_seq_buttons` / `evidence_buttons_row` 断言迁到 `ImageViewer` 侧（2 条新用例） |
| 13 | `ui/widgets/data_source_panel.py` | **修真实缺陷（非用户报告，实现自检发现）：`inputs_changed` 从未发出。** 原写法 `edit.textChanged.connect(self.inputs_changed.emit)` —— `QLineEdit.textChanged` **只有 `(str)` 一个重载**，PySide6 无法截断参数 → `TypeError: inputs_changed() only accepts 0 argument(s), 1 given!`，信号**一次都没发出去**。改为经 Python 槽 `_on_edit_changed(_text)` 转发。⚠️ 同时实测确认 `QPushButton.clicked` 因有 `clicked()` / `clicked(bool)` **双重载**，同样写法**恰好能用**（PySide6 自动挑 0 参那个）→ **两条路径不可"统一风格"，不能照抄按钮的写法**；用 `_probe_sig.py` 实测证伪了"普遍模式"的猜测，避免了一次错误的批量改动 |
| 14 | `ui/widgets/image_viewer.py` / `ui/widgets/workbench_card.py` / `ui/widgets/flow_layout.py` | **修真实缺陷（非用户报告，实现自检发现）：清空布局后旧控件成残影。** `QLayout.takeAt(0)` 只把控件从**布局**摘掉，控件仍是父级**子控件**，而 `deleteLater()` 是**异步**的（下一轮事件循环才析构）→ 旧控件继续按旧坐标渲染（图号按钮出现两份）。`_clear_thumb_buttons` / `_clear_layout` 改为 **`hide()` + `setParent(None)` 同步解除可见性**、再 `deleteLater()` 回收；`flow_layout.py` 补上"调用方须显式 hide + setParent(None)"的警告注释 |

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **1275 用例 / 0 失败 / 0 错误 / rc=0**（v0.3.6 = 1232；本版 = `test_v037_workbench.py` **+41 新锁** + `test_ui_workbench_d_class.py` 迁移拆出 2 条 = **+43**） |
| 静态检查 | `ruff check .` All checks passed |
| 真实样本基线 | `test_real_sample_sa26090215.py` **35 passed** —— ✅7 / ❌7 / ⚠️4 / 🔵0 **零漂移**（判定链路未动，符合预期） |
| UI 版式验收 | 本沙箱**浏览器不可用** → 走 Qt **离屏渲染成 PNG + 人工看图**（`tools/ui_preview.py --hit/--reviewed`），四条需求逐条肉眼确认（见下） |
| 只读红线 | `原始输入` 跑批前后一致（本版未跑批，未动任何输入文件） |

> **数字勘误（可追溯）**：本节早前记为「38 条新锁 / 1272 用例」，那是**补改动 13 / 14 两条缺陷的回归锁之前**的快照。
> 补锁后实测 **`test_v037_workbench.py` = 41 条函数**、全量 **1275 dots / 0 失败标记 / rc=0**（本机 pytest 摘要行会被吞，
> 故以 rc + dots 为取证方式）→ 以上表内数字已按下述复跑结果更新：`ruff` exit 0、真实样本基线 **35 passed** exit 0。

**逐条肉眼验收结论**（证据图：`过程产出\v0.3.7\v0.3.7_UI验收_命中态.png` / `_重判态.png`）：

| 需求 | 实测所见 |
|---|---|
| 1 | 切换条渲染为 `◀ 上一张 │ 图1 图2 图4 图6 │ 下一张 ▶ │ ★ 本图命中申报要素`，**位于图片正下方**，与图片同处查看器固定区域 |
| 2 | `图2 / 图4 / 图6`（命中）**红底白字**；`图1`（未命中）**白底蓝字**主题色 |
| 3 | 显示图 2 时，图片**右上角**出现红色 `★`（半透明白圆底） |
| 4 | 对 ❌ 条改判为 ✅ 后：状态标签 `成功 2 / 失败 0`；记录表「复核」列出现**红色加粗「人工重判」**；右卡「当前判定：✅ 校验合格」+ 复核备注；底部提示「已人工改判，再次保存将覆盖本轮留痕（幂等：以最终一次为准）」 |

### 已知局限 / 诚实披露

- ✅ **交付包已重打**（原记"未重打"已在 2026-09-17 11:44 闭环）：
  `dist\报关申报要素校验工具_v0.3.7_0917.zip` = **157,672,183 字节（150.4 MB）**，
  sha256 `cce44a8d61f86f67684415c8d0471d9436653c15705fdbcf9ced93f9510d2ed2`；731 条目 / 顶层 1 目录 / 根下 exe 就位 / testzip OK。
  `tools/smoke_frozen.py` 六项 **全绿 exit 0**（"全部关键项通过，产物可交付"）：onedir 721 文件 / 361,444,471 B、
  exe 10,396,766 B、`--self-test` exit 0 / 0.37s、`--check-env` exit 0 / 0.72s、`rules` 8/8 与源码 **sha256 逐个相同**、
  版本四者一致 = 0.3.7、冻结态运行日志含「**v0.3.7 启动**」（权威证据：跑的就是本版）。
  打包耗时 **1 分 20 秒**（`dist\` 下当时无旧 onedir 目录 → COLLECT 无需清 344 MB，是历次最快）。
  证据：`过程产出\v0.3.7\v0.3.7_打包日志.txt`（42,708 B）、`v0.3.7_打包后取证.txt`、`v0.3.7_冻结态交付核对.txt`（已按计划覆盖为打包后结果）。
- **切换条在窄窗下会被命中提示挤压**：`★ 本图命中申报要素` 与图号按钮同排，图很多 + 窄窗口时
  图号区会被压窄（`FlowLayout` 会自动换行，不会丢按钮，但可能多占一行）。
- **`--hit` 模式改写了夹具图片路径**（写成 `_ui037\_fixture_images\` 下的临时 PNG），
  仅发生在预览工具的进程内内存对象上，**不触碰项目空间文件**。
- **需求 4 的"结果表标识"只写入复核后版本**：原始三产物**刻意保持 13 列、无标识**
  （用户裁定的 13 列冻结红线 + 「不覆盖原系统产生的结果」两条要求的交集解法）。
- **待办 10（UI-1 ~ UI-4）未处理**：本轮四项需求与其无交集，`resume_run_clicked` 无发射点等
  4 条 UI 接线缺陷**原样保留**，仍待用户裁定是否另开版本修复。

### 归档

`过程产出\v0.3.7\`：`v0.3.7_全量测试与静态检查.txt`、`v0.3.7_UI验收日志.txt`、
`v0.3.7_UI验收_命中态.png`、`v0.3.7_UI验收_重判态.png`、
`v0.3.7_冻结态交付核对.txt`（**打包后**六项全绿结果）+ `v0.3.7_冻结态交付核对_prepack.txt`（打包前快照，含 3 项致命，仅作留痕）、
`v0.3.7_补锁后复跑与操作手册核对.txt`（全量 1275 dots / ruff / 基线 35 passed / 手册构建与核对四项合并）、
`v0.3.7_操作手册_v0.3.7_模板.html`（构建源，含图片占位符）、
`v0.3.7_打包日志.txt`（42,708 B，含 STEP 1–6 全流程与 PyInstaller 明细）、
`v0.3.7_打包后取证.txt`（zip sha256/条目/根下 exe + onedir 明细 + rules 8/8 sha256 比对 + 只读红线复核）。

---

### 追加交付：**面向一线员工的操作手册 v0.3.7**

> **性质：非代码变更**。`core/` / `app/` / `rules/` / `ui/` / `tests/` **零改动**（本节的改动 13 / 14 属代码，已在上表列明）。

**用户指令**（四条硬约束）：
> ①「此操作手册是给**一线员工**使用，**不要有任何技术上的代码呈现**」
> ②「操作手册尽量简单，比如如何分发，直接告诉用户将压缩包打包给想要分发的对象，对方**解压后直接双击 exe 文件即可运行**使用」
> ③「如何使用也尽量简单，如果能截图就提供 **2 张截图**（一张是首页，一张是人工复核工作台），**告知用户操作顺序**即可」
> ④「尽量做到**清晰简单明了**，一线用户**没有任何计算机和软件设计经验**，务必做到小白能看懂，他们**不需要了解内部的原理和逻辑**」

**交付物**：

| 文件 | 落点 | 说明 |
|---|---|---|
| `操作手册_v0.3.7_0917.html` | `成果产出\` | **单文件自包含**（两张截图以 base64 内嵌，共 358,295 B），可直接转发，不依赖任何外部文件 |
| `操作手册插图\01_工具首页.png` / `02_人工复核工作台.png` | `成果产出\` | 两张截图的独立副本（1440×880），便于另作 PPT / 微信图片使用 |

**内容结构（7 章，全部小白化）**：① 这个工具是干什么的 ② 怎么装：收到压缩包后 3 步
③ 怎么用：看图照做 5 步 ④ 结果文件放在哪里 ⑤ 人工复核怎么做 ⑥ 四种结果分别是什么意思 ⑦ 遇到问题怎么办。

**「剥离技术呈现」的量化核对**（脚本 `_check_manual.py`，留痕 `v0.3.7_操作手册_构建与核对.txt`）：

| 项 | 旧 v0.3.6 手册 | 新 v0.3.7 手册 |
|---|---|---|
| `class="src"` 出处标签 | 203 处 | **0** |
| `<code>` 标签 | 426 处 | **0** |
| `<pre>` 代码块 | 3 处 | **0** |
| 正文体量（不含内嵌图） | 68,571 字符 | **17,657 字符**（约 1/4） |

**新增的现场价值点**：
- **分发**压缩为「拿到 zip → 解压 → 双击 exe」三步，并给出两条不许犯的错（不在压缩包内直接双击 / 不放在会被清理的位置）；
- **2 张实拍截图 + 红圈编号图例**，编号与正文步骤一一对应（首页 ①选表 ②选图 ③预检 ④开始；工作台 ①选记录 ②看图翻页 ③看依据改结论 ④导出复核后版本）；
- 把 v0.3.7 四项优化的**用户可见部分**转译成白话：红色图号 + 右上角红星 = 本图命中；记录表红色「人工重判」= 已被人工改过；**「保存重判」按下即落盘**；复核**另存 `_复核后` 版本、原系统产物不被覆盖**；
- **纠正了旧手册的一处事实错误**：旧文写「点结果总览里的路径即可打开文件夹」，实际该处只是**可选中的文本**（`summary_panel.show_artifacts` 只 `setText`，无 `QDesktopServices`），新手册改为「显示完整路径，照着路径去找」；
- 明确「⚠️ / 🔵 不是有问题，是看不清」，避免一线把待复核当成异常追责。

**手册自身的验收**：`_check_manual.py` 全部 OK —— 2 张图 base64 解码后 PNG 头有效且均为 1440×880；
8 项技术痕迹扫描（`.py` 文件名 / `src` 标签 / `code` / `pre` / 函数调用括号 / `yaml` / 命令行参数 / 未替换占位符）
**全部 0 处**；8 项必备内容（zip 名、双击、运行目录名、`result`、导出按钮名、四种判定文案）**全部命中**。
⚠️ 核对脚本首版把 CSS 变量 `--accent` 误判成命令行参数，已把判据改为具体 CLI 旗标白名单（避免假告警进入证据链）。

---

## 操作手册交付 + UI 缺陷发现 · 2026-09-17（作者：mikiye）

> **性质**：**非代码变更**。`core/` / `app/` / `rules/` / `tests/` **零改动**；`APP_VERSION` 仍为 `0.3.6`。
> **指令**：「针对目前最终成果形成一份操作手册，包括如何分发、如何使用、如何复核、如何将复核结果进行
> 导出、最终产出目录在哪、可能碰到哪些问题、应该如何解决等，作者标记为：mikiye」。

### 新增交付物

| 文件 | 落点 | 内容 |
|---|---|---|
| `操作手册_v0.3.6_0917.html` | **`成果产出\`** | 六章操作手册：分发 / 使用 / 复核 / 复核结果导出 / 产出目录 / 问题与解决，附「口径与红线速查」+「出处索引（文件:行）」 |

- 编制方式：**逐行核对源码**（本轮精读 18 个文件），每条结论配 `文件:行` 出处索引，
  全文 **203 处** `class="src"` 出处标注；不凭记忆、不凭上一代文档。
- ⚠️ `docs/02_使用说明_v2.0.md` 属**上一代（agent 驱动）文档**，与当前交付的 PySide6 桌面 exe
  不是同一形态，**不可直接复用**，故手册按 GUI 源码重写。
- 自检留痕（过程产出）：`操作手册_自检脚本_v0.3.6_0917.py`、`操作手册_自检结果_v0.3.6_0917.txt`
  （标签配平 PASS、30/30 关键内容覆盖 OK）。

### ⚠️ 编制过程中**核出的待修缺陷**（**仅记录，未修**）

> 手册在核对「如何续跑」时发现界面交互与设计意图不一致。因涉及 UI 接线与重新打包
> （150 MB 交付物），本轮**只记录不改动**，待用户裁定后另开版本修复。

| # | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| **UI-1** | `resume_run_clicked` 信号**只被声明与连接，全 UI 无任何 `emit()`** → 界面上的「读断点续跑」入口**不可达** | 声明 `ui/widgets/data_source_panel.py:86`；连接 `ui/main_window.py:211`；全仓库检索无 `resume_run_clicked.emit()` | 中止 / 断连后无法「跳过已完成记录续跑」，只能全量重跑（OCR 缓存托底，**结果不受影响**）。而 `app/run_controller.py:308-343` 引擎侧 `resume_run()` 实现完备 |
| **UI-2** | `IDLE_WITH_BREAKPOINT`（有未完成断点）时，暂停位置按钮显示「⏸ 暂停」且**置灰**，与 `ui/main_window.py:14-18` 禁令 2 所述「暂停按钮 =『▶ 继续执行』」不符 | `ui/widgets/data_source_panel.py:336-364` | 有断点时无任何可点入口触发续跑 |
| **UI-3** | `PAUSED` 时**两个按钮文案都是「▶ 继续执行」**，但主按钮的路由是「有断点→重新开始 / 无断点→开始」，程序仍在运行时点它会被 `_launch()` 直接忽略 | `ui/widgets/data_source_panel.py:307-319`、`336-364`；`app/run_controller.py:409-411` | 用户易按错按钮；现场表现为「点了没反应」 |
| **UI-4** | 「⏹ 中止」后状态栏提示「可『继续执行』续跑」，但该入口当前不可达 | `ui/main_window.py:486-487` | 提示与可点入口不一致，误导现场 |

- 上述 4 条**已写入操作手册**（`§2.5 / §2.6 / §6.3`），并给出**现场可行的替代操作**
  （中止后点「↻ 重新开始」，依赖 OCR 缓存降低重跑代价），确保手册在现状下可用。
- 修复方向（**未实施**）：把 `IDLE_WITH_BREAKPOINT` 下暂停位置按钮的文案改为「▶ 继续执行」并接
  `resume_run_clicked`；`PAUSED`（worker 已结束）时主按钮接 `resume_run` 而非 `start`；
  同步修正中止后的状态栏文案；补 `tests/` 回归锁。**须经用户确认后另开版本实施 + 重新打包**。

## 项目空间归档 · 2026-09-17（保留最新版本，历史版本成果迁入 `归档\`）

> **性质**：**非代码变更**。源码、判定口径、测试基线**零改动**；仅调整项目空间的文件布局。
> **指令**：「保留最后一个版本，将历史版本成果挪到归档目录」。

### 规则

| 类别 | 处置 |
|---|---|
| 最新版本 **v0.3.6** 全部产物 | 原地保留（`成果产出\` / `过程产出\` / `dist\`） |
| 历史版本 **v0.2.0 / v0.3.0 / v0.3.1 / v0.3.3 / v0.3.4 / v0.3.5** 产物 | 迁入 `归档\`（**只搬运，不改内容，不删除**） |
| 版本无关的证据链（`docs/09b` §8.3 白名单） | **原地保留，零搬迁** |

### 迁出清单（19 项 / 1,668,889,174 字节 ≈ 1.55 GB）

| 归档目标 | 内容 | 体量 |
|---|---|---|
| `归档\dist\` | 6 个历史版本 zip（v0.2.0 / v0.3.0 / v0.3.1 / v0.3.3 / v0.3.4 / v0.3.5） | 945,870,436 B |
| `归档\dist\` | 2 个 onedir 残壳目录（`报关申报要素校验工具091703\`、`报关申报要素校验工具091704\`） | 722,850,756 B |
| `归档\成果产出\` | `报关申报要素校验_v0.2.0_交付报告_0916.html` | 20,070 B |
| `归档\过程产出\v0.3.3\|v0.3.4\|v0.3.5\` | 对应版本的打包日志 / 冻结态交付核对 / 缺陷复现与修复验证 / 测试与静态检查 / 申报侧解析比对（共 10 项） | 147,912 B |

> `报关申报要素校验工具091703\` / `091704\` 的来源：PyInstaller COLLECT 阶段**清理旧输出目录失败**时遗留的 onedir 残壳
> （`过程产出\v0.3.6_打包日志.txt` STEP 5「核对产物」中即列出 `报关申报要素校验工具091703\  (目录)`）。
> 交付形态始终是 `dist\` 下那一个 zip，这两个目录不是交付物。

### 复核（可证伪）

| 项 | 归档前 | 归档后 | 结论 |
|---|---|---|---|
| `原始输入\` | 130 文件 / 394,140,543 B | **130 文件 / 394,140,543 B** | ✅ 逐字节一致（「只进不改」铁律守住） |
| `customs_declaration_checker\dist\` | 1,826,377,844 B | **157,656,652 B**（仅 `…_v0.3.6_0917.zip`） | ✅ 释放 1.55 GB |
| `成果产出\` | 7 文件 | **6 文件** | ✅ 仅 v0.2.0 报告迁出 |
| `过程产出\` 顶层 | 43 项 | **33 项** | ✅ 仅版本号条目迁出，证据链全在 |

- 迁移用 `System.IO.File/Directory.Move()`（同卷 rename，瞬时），**逐项 `try/catch` + 事后 `Test-Path` 复核**：
  实测 **`MOVED=19  FAILED=0`**。
- 归档索引：`归档\README_归档索引.md`（含逐文件字节数、恢复方式、为何部分文件不搬）。
- 同批清理（项目空间）：`_pb.txt`（18 B 临时残留）、`.ruff_cache`（2 文件 / 78 B 可重建缓存）。

> ⚠️ **路径漂移提示**：本文件 §[0.3.5]、§[0.3.4]、§[0.3.3] 中「分发包」「归档」两行记录的路径，
> 现已由 `dist\…zip` / `过程产出\…txt` 变为 `归档\dist\…zip` / `归档\过程产出\vX\…txt`。
> **记录内容不变，仅位置迁移**；以本节 + `归档\README_归档索引.md` 为准。

### 未改动（红线自检）

- `APP_VERSION` 仍为 `0.3.6`；`core/` / `app/` / `rules/` / `tests/` **零改动**。
- 回归基座 `过程产出\校验详细日志_SA26090215_累积.json` **原地未动**（`tests/test_real_sample_sa26090215.py`、
  `tools/eval_v03.py` 仍直接按原路径读取）。
- 四类判定口径、13 列汇总表结构、`docs/09b` §8.3 证据链白名单 —— 全部未动。

---

## [0.3.6] · 2026-09-17（汇总表第 10 列：**「差异备注」→「判定依据」**，四类结论均落依据）

> **触发（用户裁定 2026-09-17，原文）**：
> 「导出结果将差异备注改为：判定依据 —— 包括成功、失败、质疑的依据是什么均列在此一栏，
> 而不是仅仅只是落盘差异的依据」
>
> **性质判定：导出层「列名 ＋ 内容口径」变更，非判定逻辑变更。**
> 四类判定字符串（SOP 1.3 逐字）、两级分流、整机品牌口径、`SUSPICIOUS` 降级护栏、
> **`core/judge_engine.py` 判定链路零改动** —— 判定结论一个字都没变；
> 变的只是"把已有的依据**完整写进汇总表那一格**"。

### 现象与口径变化

| 项 | 修复前 | 修复后 |
|---|---|---|
| 列名（第 10 列） | `差异备注` | **`判定依据`** |
| ✅ 校验合格 行 | **整格为空**（无差异 → 无备注）→ 合格记录**拿不到依据，无法追溯** | `品牌与型号双方均为『无』，判合格` |
| ❌ 校验异常 行 | 只有差异明细（字段级对照 + 逐字符差异） | 结论句 ＋ 差异明细（＋ 复核备注） |
| ⚠️/🔵 待复核 行 | 只有差异明细（🔵 常整格为空） | 结论句（「疑似 OCR 噪声…转人工复核」/「…未找到图片」）＋ 差异明细 |
| 列数与顺序 | 13 列 | **13 列（不变）** |
| 判定结论 | — | **完全不变** |

**根因（口径缺口）**：原列语义是"**落盘差异**的备注"，与「可追溯红线」是两回事 ——
判定为 ✅ 的记录恰恰**没有差异可落**，于是这一格永远是空的。
用户在复核时看不到"**为什么**判合格"，只能去翻详细 JSON 或逐条重新推理。

### 改动清单

| # | 位点 | 修法 |
|---|---|---|
| 1 | `core/constants.py::COLUMNS[9]` | 列名 `差异备注` → **`判定依据`**；补 v0.3.6 口径说明块（**列数与顺序不变**） |
| 2 | **新增** `core/models.py::CheckResult.verdict_basis()` | 「判定依据」文本的**唯一生产者**：`reason`（结论句）＋ 差异明细（含逐字符差异）＋ `复核备注：…`，以 `；` 连接 |
| 3 | `core/models.py::CheckResult.to_row()` | 第 10 位改为 `self.verdict_basis()`，原内联拼接整段删除 |
| 4 | `core/models.py::DifferenceDetail.summary()` | 签名扩展为 `summary(*, include_head=True, include_note=True)` —— 对照句/备注已在 `reason` 里时**可分别抑制**，避免同格重复 |
| 5 | `core/result_exporter.py::_review_row()` | 「问题说明」**改走同一出口** `result.verdict_basis()`，删掉原有的 `reason + d.summary()` 二次实现 |
| 6 | 全仓字样同步 | `diff_util` / `noise_guard` / `judge_engine` / `none_marker` / `token_matcher` / 两个 YAML / `docs/01` `02` `03` / 4 个测试文件 —— 注释与文案里的「差异备注」统一改为「判定依据」 |
| 7 | `tests/test_exporter.py` | 新增 `TestVerdictBasisColumn`（**10 个用例**）+ `TestReviewCsv::test_problem_column_shares_basis_exit`（1 个） |

⚠️ **为什么要有 #4/#5**：`judge_engine` 会把字段级依据**同时**写进 `reason` 与
`DifferenceDetail.note`。v0.3.6 首轮端到端实测直接把两段拼在一格 → **同句重复 2–5 遍**
（如 `[1]` 行「品牌：申报『baori』与图片『Daewoo』明确不一致」出现两次）。
去重规则三条：① `note` 已出现在 `reason` → 不再附注；② 字段级对照（申报值 ＋ 图片值）
已由 `reason` 表达 → 只补「差异点」，不重复对照句；③ ①②都冗余且无逐字符差异 → 整条跳过。

⚠️ **为什么必须做成单一出口（#2）**：汇总表第 10 列与复核清单「问题说明」是**同一个语义**
（"为什么给这个结论"）的两个落点。首轮实现两处各写一遍，就同时命中同一个重复缺陷 ——
`verdict_basis()` 收口后由 `test_problem_column_shares_basis_exit` 锁死"两边逐字相同"。

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **1232 用例 / 0 失败 / 0 错误**（v0.3.5 = 1221，本版 **+11**） |
| 静态检查 | `ruff check .` All checks passed |
| 真实样本基线 | `test_real_sample_sa26090215.py` **35 passed** —— ✅7 / ❌7 / ⚠️4 / 🔵0 **零漂移**（判定链路未动，符合预期） |
| 端到端（真实导出器） | 用真实 `ResultExporter` 从 SA26090215 快照重出 `校验汇总表_SA26090215.xlsx`：**18/18 行的第 10 列非空**，四类结论均有依据（`过程产出\v0.3.6_判定依据列端到端核验.txt`） |
| 表头核验 | 第 10 列列名 = `判定依据`；列数 = 13 |
| 同句重复 | 首轮复现 → 去重后逐行复核，**同句重复清零** |

**✅ 行实测样例**：`品牌与型号双方均为『无』，判合格`（此前为空）。
**❌ 行实测样例**：`品牌：申报『baori』与图片『Daewoo』明确不一致；品牌：差异点：『申报b』→『图片D』（第0位起）图片多出『ew』（第2位起）『申报ri』→『图片o』（第3位起）（外箱整机品牌，不构成本体证据）`。

### 已知冗余 / 已知局限（诚实披露）

- **第 10 列 ⊇ 第 13 列**：第 13 列「判定说明」＝ `reason`（一句话判定理由），
  而 `reason` 正是第 10 列「判定依据」的**首段** → 无差异时两格内容**逐字相同**
  （`test_exported_sheet_basis_not_empty_for_pass` 已显式固化该事实）。
  这是**用户要求的必然结果**（"依据列必须自足"），故**刻意保留**：
  第 13 列保持"一句话速览"，第 10 列给出"完整依据链"。
  ⚠️ 13 列结构已冻结（用户裁定），**不得**为消冗余而删列/改列序。
  若用户希望第 13 列改为短标签（如仅「合格 / 异常 / 待复核」），需另行裁定。
- **判定链路零改动**：本版不修判定，故 v0.3.5 的「已知残余」（混排被拒 + 图内有显式
  「无品牌」→ 判 ✅）**原样继承**，本版未扩大也未收窄。

### 复核材料（追加，2026-09-17）

> **触发（用户要求，原文）**：
> 「将目前产出的判断逻辑树进行梳理形成一份判断逻辑流程图进行输出，我需要用这个来进行复核，
> 同步输出落盘逻辑说明的markdown文件」

**性质：只出复核文档，源码零改动**（不改判定、不改落盘、不改列口径）。

| 交付物 | 落点 | 内容 |
|---|---|---|
| `判定逻辑流程图_v0.3.6_0917.html` | 项目空间 `成果产出\` | 9 章 / **7 张流程图** / 5 张表格 / 10 项复核检查清单 / 22 条依据出处索引；暗浅双主题 |
| `落盘逻辑说明_v0.3.6_0917.md` | 项目空间 `成果产出\` | 10 章 / 29 条出处索引；目录锚点 · 三产物 · 断点 · OCR 缓存 · 复核留痕 · 越界断言 · 自动暂停 · 落盘时序 · 编码约定 |
| `判定逻辑流程图_生成脚本_v0.3.6_0917.py` | 项目空间 `过程产出\` | 可复现生成器（事实来源 = 逐行核对源码；改数据不改版式） |

**7 张图**：A 判定主流程 · B 字段决策树（申报有值）· B′ 字段决策树（申报为无）·
C 分词命中三级 + 字段名护栏三道 · D 噪声三态判据链 · E 结论组装 · F 落盘逻辑。

#### ⚠️ 本沙箱无法启动浏览器 → 改用「几何审计」做版式验收

`chrome.exe --version` **无任何输出**、`--screenshot` 静默不写文件 ——
逐图截屏肉眼核对这条路在本环境**走不通**（全程仅首幅 `fig_A_dark.png` 成功，已人工核对，
作为"生成器版式模型正确"的旁证）。

改为把生成的 SVG 解析回来，用**与生成器相同**的字宽模型重建每个文本包围盒，
对四类缺陷做可证伪的数值断言：**① 文本溢出盒子 ② 文本框互叠 ③ 矩形盒互叠 ④ 越出画布**。

- **首轮抓到 2 处真实缺陷**（肉眼未必看得出）：
  图 B「否」标签与盒 `(40,404 440x62)` 重叠 **1.8px**；
  图 C「通过」标签与盒 `(40,90 500x62)` 重叠 **0.8px**。
  根因：箭头标签默认落在线段**上方**（`label_dy=-5`），竖直箭头两侧都是盒子，上偏即擦边。
  修法：标签默认位置改到线段**下方**（`label_dy=+3`），并在 `arrow()` 写明原因。
- **另手工发现并修 3 处**：图 B′ 的 5 条注释压在 `BOTH_ABSENT` 盒上（整体下移 50px）；
  图 C 注释压住 `FUZZY-②` 下沿（下移并加高画布）；图 F 存在一条**零长度箭头**
  `(500,242)→(500,242)`，改为「根框 → 左右分叉」的正确连接。
- **修复后**：**7/7 图全部 PASS**（盒 72 / 文本 187 个全部落在留白内），
  生成器静态溢出告警 **0 条**；HTML 结构自检 16 项全过。
- **顺带修掉一处生成器隐患**：原 `box()` 用传入 `fs`（可为 12.0）估算宽度，
  而渲染字体来自 CSS 类 `.tt`（12.5px）—— "按 12.0 量、按 12.5 画"存在 **4.2% 系统性低估**。
  改为给每个 `text` 写显式 `style="font-size:{size}px"`，使估算与渲染同口径。

#### 落盘说明中修正的一处事实

`infra/config.py` 的 `result_dir` / `process_dir` **已是兼容占位字段**
（保留仅为防止旧版 `config.json` 反序列化崩溃，**不读取、不写生效值**；
UI 上两行控件已整行移除）。输出落点**完全**由
`app/path_policy.py::PathPolicy.resolve_outputs()` 的运行目录约定决定 =
`anchor/报关申报要素校验/{result,logs}`，`anchor` 优先级
**显式 base > 显式 app_home > `app_base_dir()`**（打包态 = exe 所在目录）。

> ⚠️ 项目空间现存的 `成果产出\` / `过程产出\` 是**开发期显式导出/归档的历史产物**，
> **不是交付包的默认落点** —— 交付包不会去写项目空间。说明文档已明确区分，
> 免得复核时按错路径找文件。

#### 归档

- `过程产出\v0.3.6_判定逻辑流程图与落盘说明_生成与校验.txt`（几何审计 + 结构自检完整结论）
- `过程产出\v0.3.6_流程图渲染旁证_figA_dark.png`（唯一成功的真实渲染截图）

### 打包与冻结态交付核对（v0.3.6）

| 项 | 结果 |
|---|---|
| 前置体检 | `tools/check_env.py` **exit 0**（19 依赖 + 23 模块 import + 无空壳包） |
| 打包耗时 | **2 分 49 秒**（打包前已清 `PYTHONPATH` → COLLECT 清旧 onedir 未触发 fail-closed） |
| onedir | 721 文件 / 361,427,582 字节（344.7 MB）；exe 10,379,877 字节 |
| 分发包 | `dist\报关申报要素校验工具_v0.3.6_0917.zip` —— **157,656,652 字节（150.4 MB）** |
| sha256 | `2fdb21945abf674a2f0350e8a799a87c67ea91ff7d6bd1014e92069ce61d04b2` |
| zip 结构 | 731 条目 / 顶层仅 1 个目录 / 根下 exe 就位 / `testzip` OK |
| 冻结态冒烟 | `--self-test` **exit 0 / 0.52s**（运行日志含 `v0.3.6 启动`）；`--check-env` **exit 0 / 0.84s** |
| 规则收全 | `_internal\rules\` **8/8**，且与源码 **sha256 逐个相同**（0 差异） |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = zip 文件名 = **0.3.6** |
| 改动模块进包 | `build\customs_checker\PYZ-00.toc` 含 `core.models`（**本次改动模块**）/ `core.constants` / `core.result_exporter` / `core.element_parser` / `core.judge_engine` / `core.none_marker` / `core.noise_guard` / `core.token_matcher` |
| 交付核对 | `tools/smoke_frozen.py` 六项【结论】✓ 全部通过（exit 0） |
| 只读红线 | `原始输入` 跑批前后一致：**130 文件 / 394,140,543 字节** |
| 归档 | `过程产出\v0.3.6_打包日志.txt`、`v0.3.6_冻结态交付核对.txt`、`v0.3.6_判定依据列端到端核验.txt`、`v0.3.6_全量测试与静态检查.txt` |

> ⚠️ `dist\报关申报要素校验工具_v0.3.5_0917.zip`（及更早的 v0.3.4 / v0.3.3 / v0.3.1 / v0.3.0 / v0.2.0）
> **已被本版取代**，现场若已分发需换包。旧包**未删除**（待用户裁决）。

---

## [0.3.5] · 2026-09-17（品牌「分词边界」缺陷修复：邻座要素值污染「无品牌」）

> **触发（现场报告）**：申报要素原文
> `材质：纸制|形状：L形,条状|规格：1220mm*(50mm+50mm)*5mm 无品牌`，
> 品牌被识别为 **`1220mm*(50mm+50mm)*5mm 无`** —— 属**分词识别异常**。
>
> **用户裁定（作为分词识别规则）**：
> ① 「`无品牌`」应该是**一个完整分词**，`1220mm*(50mm+50mm)*5mm` 应该是**另外一个分词**；
> ② **品牌和型号不会出现中英文混合词**。
>
> **性质判定：实现缺陷修复，非口径变更。** 四类判定字符串、13 列结构、两级分流、
> 整机品牌口径、`SUSPICIOUS` 降级护栏、`core/judge_engine.py` 与 `core/none_marker.py`
> **零改动**；缺陷全在解析侧的分词环节。

### 现象与根因

| 层 | 修复前 | 修复后 |
|---|---|---|
| `split_fields` 段落 | 键=`规格`、值=`1220mm*(50mm+50mm)*5mm 无品牌` | 同左（空格**不是**分隔符，行为未变） |
| `ElementParser.parse(...).brand` | `'1220mm*(50mm+50mm)*5mm 无'` | `''`（等价「无」） |
| `is_none_token(brand)` | `False` → **显式无标记链路整条被跳过** | `True` |
| 判定结论（图内显式 `无品牌`） | ⚠️ 缺图内标识，人工复核 | **✅ 校验合格** |

**根因**：`_match_brand_value` 的 ② 分支（「品牌」**后缀**形态：`宇同品牌` / `无品牌`）
语义是"紧挨着『品牌』之前的**那个词**就是品牌值"，实现却是取**整段**前缀
（`candidate.split("品牌", 1)[0]`）。而 `rules/separators.yaml` 只把
`、`/`;`/`；`/`/`/`|` 归一化为分隔符，**空格不是分隔符** —— 于是
`规格` 这个**别的要素**的取值与品牌要素值被空格连写后，整段落进品牌值：

```
材质：纸制|形状：L形,条状|规格：1220mm*(50mm+50mm)*5mm 无品牌
                                    └──── 规格的值 ────┘└品牌值┘
split("品牌")[0] =  1220mm*(50mm+50mm)*5mm 无     ← 脏值（非「无」类取值）
```

与该脏值配套的连锁反应与 v0.3.4 完全同型：`is_none_token()` 失效 →
`judge_engine._annotate_none_marker` 的前置条件不成立 →
「申报为无 ＋ 图内显式『无品牌』→ 核验通过」（口径 v0.3.2）整条链路被跳过。

### 修复（2 个新护栏 + 1 处对称加固）

| # | 位点 | 修法 |
|---|---|---|
| 1 | 新增模块级纯函数 `narrow_suffix_token()` | **空格即分词边界**：后缀形态（`X品牌` / `X牌`）只取**紧邻后缀词的最后一个分词**（`1220mm*(50mm+50mm)*5mm 无` → `无` → 判「无」）。②③ 两个分支**对称**调用 |
| 2 | 新增 `is_mixed_cjk_ascii()` ＋ `ElementParser._finalize_brand()` | 用户原则「品牌/型号不会出现中英文混合词」→ 中英混排的候选串**不是一个分词**，一律不采信。`_match_brand_value` 的**四个采信位点**（② 品牌后缀 / ③ 牌后缀 / ④ 正则捕获 / ⑤ 裸 token）**统一**经 `_finalize_brand` 收口 |
| 3 | `_extract_model_from_value` 加混排否决 | **型号侧对称加固**（用户原则对型号同样成立），清洗后仍中英混排 → 判空 |

⚠️ **为什么加"收口"这一层**：v0.3.4 的根因是**品牌侧/型号侧护栏不对称**；
同一类风险在**分支之间**同样存在（只在 ② 加护栏，③④⑤ 就是下一个漏口）。
`_finalize_brand` 把"黑名单 + 分词合法性"固定成唯一出口 —— **新增采信分支必须调用它**。

⚠️ **不新增 YAML 规则**：两条都是**结构性不变量**（"空格是分词边界"、"一个分词不中英混排"），
不是可配置清单；与既有代码级常量 `_BRAND_TOKEN_RE` / `_DEFAULT_INVISIBLE` 同类。
落在 `core/element_parser.py` 并导出为 `__all__` 成员（`is_mixed_cjk_ascii` /
`narrow_suffix_token`），供单测直接断言。

### 不虚高 / 不误伤双向反向锁

| 方向 | 用例 | 断言 |
|---|---|---|
| **不误伤**（品牌名内部空格） | `NEW BALANCE品牌` / `DAEWOO ELECTRONICS品牌` | `narrow_suffix_token` 对**纯 ASCII 多词**原样返回 → 品牌仍取全名 |
| **不误伤**（紧邻后缀的真实品牌） | `电视机用 创维品牌` / `外壳 ABS 海尔品牌` / `尺寸 120mm SKYWORTH品牌` | 收窄后仍取到 `创维` / `海尔` / `SKYWORTH` |
| **不虚高**（纯数字不否决） | `is_mixed_cjk_ascii("1220")` | `False` —— 混排护栏只判「汉字 + ASCII **字母**」，纯数字交给黑名单/正则 |
| **不虚高**（真实值不被混排误杀） | `品牌:无锡机电` / `品牌:无极` / `品牌:无绳吸尘器` | 逐条保持（v0.3.4 反向锁） |
| **反证锁** | 手工传入 `brand='1220mm*(50mm+50mm)*5mm 无'` | **必须判不出 ✅**（证明新用例不在"恒真"路径上通过） |

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **1221 用例 / 0 失败 / 0 错误**（v0.3.4 = 1199，本版 **+22**） |
| 静态检查 | `ruff check .` All checks passed |
| 真实样本 SA26090215 | **基线零漂移**：✅7 / ❌7 / ⚠️4 / 🔵0（18 条守恒，`test_v030_baseline_locked` 硬断言通过） |
| 申报侧解析比对 | 18 条 `raw_element_text` 用新解析器复算，与快照逐条一致（seq=16/17/18 的"差异"是快照陈旧的 `decl_model`，测试模块注释已声明，与本版无关）；**样本内无中英混排的申报值** → 混排护栏在真实样本上是空转（零副作用） |
| 形态表探针 | 23 条书写形态逐条核验（`过程产出\v0.3.5_缺陷复现与修复验证.txt`） |
| 端到端复现 | 现场原文 ＋ 该票 OCR → `verdict = PASS`，理由写明「申报为『无』，图片中显式标注『无品牌』（图 1），双方均为『无』，判合格」 |
| 既有形态回归 | 五种品牌形态 / `品牌;无;` / `无品牌、无型号、用途：…` / `宇同品牌、无型号、…` / 缺陷 E 型号 / 反查 `品牌类型:0` **逐条不变** |

### 打包与冻结态交付核对（v0.3.5）

| 项 | 结果 |
|---|---|
| 前置体检 | `tools/check_env.py` **exit 0**（19 依赖 + 23 模块 import + 无空壳包） |
| 打包耗时 | **2 分 51 秒**（打包前已清 `PYTHONPATH` → COLLECT 清旧 onedir 未触发 fail-closed） |
| onedir | 721 文件 / 361,426,141 字节（344.7 MB）；exe 10,378,436 字节 |
| 分发包 | `dist\报关申报要素校验工具_v0.3.5_0917.zip` —— **157,655,795 字节（150.4 MB）** |
| sha256 | `273c95925df87ee7127333417b07b7bdde4baec7759d400676dc93c3471c06a9` |
| zip 结构 | 731 条目 / 顶层仅 1 个目录 / 根下 exe 就位 / `testzip` OK |
| 冻结态冒烟 | `--self-test` **exit 0 / 0.44s**（日志含 `v0.3.5 启动`）；`--check-env` **exit 0 / 0.76s** |
| 规则收全 | `_internal\rules\` **8/8**，且与源码 **sha256 逐个相同**（0 差异） |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = zip 文件名 = **0.3.5** |
| 改动模块进包 | `build\customs_checker\PYZ-00.toc` 含 `core.element_parser`（**本次改动模块**）/ `core.none_marker` / `core.judge_engine` / `core.noise_guard` / `core.token_matcher` |
| 交付核对 | `tools/smoke_frozen.py` 六项【结论】✓ 全部通过（exit 0） |
| 归档 | `过程产出\v0.3.5_打包日志.txt`、`v0.3.5_冻结态交付核对.txt`、`v0.3.5_缺陷复现与修复验证.txt`、`v0.3.5_申报侧解析比对.txt`、`v0.3.5_全量测试与静态检查.txt` |

> ⚠️ `dist\报关申报要素校验工具_v0.3.4_0917.zip`（及更早的 v0.3.3 / v0.3.1 / v0.3.0 / v0.2.0）
> **已被本版取代**，现场若已分发需换包。旧包**未删除**（待用户裁决）。

**受影响形态清单（解析结果变化的只有这些）**：品牌要素值以「品牌 / 牌」**后缀**形式书写、
且其后缀词之前存在**空格分隔的邻座要素值**的原文；以及候选品牌/型号串**中英混排**的原文。

**已知未改（本版有意不动）**：显式键值分离形态 `品牌:NEW BALANCE` 仍会经 ④ 的
`bare_ascii_allcaps` 兜底取到 `BALANCE`（历史既有行为，v0.3.4 及以前一致）。
它属于「品牌名内含空格」的**另一课题**（`_BRAND_TOKEN_RE` 本身不允许空格），
与本轮「邻座要素污染」根因无关，故**不在本版扩大改动面**，留待专项评估。

**已知残余（诚实披露，非本版引入）**：混排候选被判空后，申报侧即呈「缺该要素」，
若同一票图内又存在显式「无品牌」标记，按口径 v0.3.2 会判 ✅。该路径**在本版之前就已存在**
（探针实测 v0.3.4 对 `品牌:创维SKYWORTH` 同样产出 `''`，因为 ④ 的正则与 `_BRAND_TOKEN_RE`
本就拒收混排），本版只是把这条既有事实**显式化**。真实样本 18 条申报要素中**无任何中英混排取值**
→ 该路径当前**无实测暴露面**。按用户原则（品牌/型号不会中英混排）保留此行为；
若现场出现「品牌确为中英混排且图内又有『无品牌』」的实际样本，须回到用户处重新裁定。

---

## [0.3.4] · 2026-09-17（申报要素「元素值连写」泄漏缺陷修复）

> **触发（现场报告）**：申报要素原文 `无品牌 无商业价值`，图内 OCR 有显式 `无品牌`，
> **应判 ✅ 通过**，实际判 ⚠️（`reason` 里出现 `品牌：图片 OCR 全文未出现完整分词『无 无商业价值』`）。
>
> **性质判定：实现缺陷修复，非口径变更。** 用户已在 v0.3.2 裁定
> 「申报为无 ＋ 图内显式『无品牌』→ 核验通过」；本版只是让该裁定在
> **「元素值连写」这一书写形态**下真正生效。四类判定字符串、13 列结构、
> 两级分流、整机品牌口径、`SUSPICIOUS` 降级护栏、`core/judge_engine.py`
> 与 `core/noise_guard.py` **零改动**（判定侧一行未动，缺陷全在解析侧）。

### 现象与根因

| 层 | 修复前 | 修复后 |
|---|---|---|
| `ElementParser.parse("无品牌 无商业价值").brand` | `'无 无商业价值'` | `''`（等价「无」） |
| `is_none_token(brand)` | `False` → **显式无标记链路整条被跳过** | `True` |
| 判定结论（图内 `无品牌` ＋ 唛头 `SKYHORTH P/N`） | ⚠️ | **✅ 通过** |

**根因（两个位点，同一类错误：把邻座要素的取值当成本要素的取值）**：

1. `rules/separators.yaml` 里 `、`/`;`/`；`/`/`/`|` 会被归一化为分隔符，但**空格不是**
   → `无品牌 无商业价值` 整段作为**一个**候选值到达品牌提取。
   `_match_brand_value` 的 ② 分支用 `candidate.replace("品牌", "")` 取前缀，
   **全局替换**后把紧随其后的**其他要素值**一并带进品牌值 → `无 无商业价值`；
2. 空格连写段里若再出现冒号（`无品牌 无商业价值 用途:电视机用`），
   `split_fields` 按首个冒号切开 → 键=`无品牌 无商业价值 用途`、值=`电视机用`；
   品牌提取的「无显式品牌字段」兜底分支取的是**值**侧 → 品牌被读成 `电视机用`。

**为什么 `无 无商业价值` 会毁掉整条链路**：它不是「无」类取值
→ `judge_engine._annotate_none_marker` 的 `is_none_token(declared)` 前置条件不成立
→ 申报侧"显式无标记"这一路证据**根本不会被挂上**（`core/none_marker.py` 本身的
图片侧标记识别是**正确的**：探针实测 `detect_none_markers → 品牌: ('brand_bare','无品牌',1,'无品牌')`）。

### 修复（3 个位点，品牌/型号对称）

| # | 位点 | 修法 |
|---|---|---|
| 1 | `_match_brand_value` 新增 ➋ 护栏 | 候选值以**某个 `none_tokens` 条目开头且后接边界**（行尾 / 空白 / 标点）→ 直接判空。新增 `_starts_with_none_token()`，**必须在 ②/③/④ 之前**：否则 ④ 的 `single_char_pai` 正则（"值 + 牌"）会把 `无品牌` 拆成 `品` |
| 2 | `_match_brand_value` 的 ② 分支 | `replace("品牌","")` → `split("品牌", 1)[0]`：只取**首个**「品牌」之前的部分，**绝不**把品牌值之后的要素值带进来 |
| 3 | `_extract_brand` 兜底分支新增 `_brand_candidate_side()` | 品牌字样落在**键**上且键≠值时，候选取**键**侧（`宇同品牌 用途` → `宇同`），不取值侧（那是别的要素） |

**型号侧对称加固（同类缺陷，一并修）**：

* `_extract_model_from_value` 新增**形态 D**：值以「无」类取值开头且有边界
  （`型号:无 长度:150MM`）→ 判空（与品牌侧 ➋ 对称）；
* `_extract_model` ② 分支：键侧已写明「无型号」本体（`无型号 用途:电视机用`）→ 立即判空，
  **绝不**取「值」侧（实测曾产出 `电视机用`）。

⚠️ 型号侧本就有一条「`无型号` 前缀 → 空」的护栏（形态 C），**品牌侧此前缺这条对称护栏**
—— 这个不对称正是本次缺陷的根因。三处修复均**不新增规则常量**（复用既有
`none_tokens` / 既有 `品牌` / `型号` 字样判据），符合「规则外置、补规则=改 YAML」的设计。

### 不虚高反向锁

| 用例 | 断言 |
|---|---|
| `品牌:无锡机电` / `品牌:无极` / `品牌:无绳吸尘器` | `is_none_token(brand) is False`（`无` 后接正文 → **无边界** → 不算「无」） |
| `无商业价值`（无品牌要素） | `_starts_with_none_token` 返回 `False` |
| `品牌` 有值 ＋ 图内「无品牌」 | 仍判 ⚠️（v0.3.2 反向锁保持） |
| 脏值反证 | 手工传入 `brand='无 无商业价值'` → **必须判不出 ✅**（证明新用例不在"恒真"路径上通过） |

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **1199 用例 / 0 失败 / 0 错误**（v0.3.3 = 1174，本版 **+25**） |
| 静态检查 | `ruff check .` All checks passed |
| 真实样本 SA26090215 | **基线零漂移**：✅7 / ❌7 / ⚠️4 / 🔵0（18 条守恒，`test_v030_baseline_locked` 硬断言通过）；seq=16/17/18 型号 `HS-8A50J-12` 仍正确 |
| 端到端复现 | 现场原文 ＋ 现场 32 行 OCR → `verdict = PASS`，理由写明「品牌：申报为『无』，图片中显式标注『无品牌』（图 1），双方均为『无』，判合格」；`SKYHORTH` 仍写进差异备注（可追溯红线） |
| 既有书写形态回归 | 五种品牌形态 / `品牌;无;` / `无品牌、无型号、用途：…` / `宇同品牌、无型号、…` / 缺陷 E 型号 / 反查 `品牌类型:0` **逐条不变** |

### 打包与冻结态交付核对（v0.3.4）

| 项 | 结果 |
|---|---|
| 前置体检 | `tools/check_env.py` **exit 0**（19 依赖 + 23 模块 import + 无空壳包） |
| 打包耗时 | **1 分 24 秒**（打包前已清 `PYTHONPATH`，未触发 COLLECT fail-closed） |
| onedir | 721 文件 / 361,423,824 字节（344.7 MB）；exe 10,376,119 字节 |
| 分发包 | `dist\报关申报要素校验工具_v0.3.4_0917.zip` —— **157,654,133 字节（150.4 MB）** |
| sha256 | `9422fdd3a4a886c51a1eb997e0fd05a226dee8f8edbe688349e3196d1849dc54` |
| zip 结构 | 731 条目 / 顶层仅 1 个目录 / 根下 exe 就位 / `testzip` OK |
| 冻结态冒烟 | `--self-test` **exit 0 / 0.44s**（日志含 `v0.3.4 启动`）；`--check-env` **exit 0 / 0.73s** |
| 规则收全 | `_internal\rules\` **8/8**，且与源码 **sha256 逐个相同** |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = zip 文件名 = **0.3.4** |
| 新增/改动模块进包 | `build\customs_checker\PYZ-00.toc` 含 `core.element_parser`（本次改动模块）/ `core.none_marker` / `core.judge_engine` / `core.token_matcher` / `core.noise_guard` / `ui.widgets.workbench_card` / `ui.widgets.ocr_text_dialog` |
| 交付核对 | `tools/smoke_frozen.py` 六项【结论】✓ 全部通过 |
| 归档 | `过程产出\v0.3.4_打包日志.txt`、`过程产出\v0.3.4_冻结态交付核对.txt`、`过程产出\v0.3.4_缺陷复现与修复验证.txt` |

> ⚠️ `dist\报关申报要素校验工具_v0.3.3_0917.zip` **已被本版取代**（v0.3.3 含本缺陷），
> 现场若已分发需换包。


**受影响形态清单（解析结果变化的只有这些）**：品牌或型号要素值以「无」类取值开头、
且其后**紧跟非分隔符连写的其他要素**的原文；以及品牌字样在键上、值属别的要素的原文。

---

## [0.3.3] · 2026-09-17（复核工作台新增「查看申报要素」弹窗）

> 触发：现场反馈 —— 复核时要看**申报要素表里的整段原文**，只能切回 Excel，
> 而卡片上只有「查看原始 OCR 文本」。**本版零口径变更**（不碰判定链路 / 不碰四类判定字符串）。

### 新增 · 入口按钮「查看申报要素」

「人工复核工作台」证据卡内，在既有按钮「查看原始 OCR 文本」**右侧紧邻**新增
「查看申报要素」（两者同一行、同一布局，由 `test_decl_button_exists_beside_ocr_button`
用「同一 QHBoxLayout 内下标相邻」硬断言锁住，而非只断言"两个按钮都存在"）。

### 新增 · `DeclarationTextDialog`（申报要素原文弹窗）

**设计取舍：复用而非另起一套定位逻辑。** 新弹窗继承既有
`ui/widgets/ocr_text_dialog.py::OcrTextDialog`，**完整复用**其定位实现
（`place()` 纯函数 + `show_beside()`），从而自动继承 v0.3.1 现场修正过的
「右侧、垂直居中、**不遮挡图片区**」行为 —— **不再重算一遍几何**，
也就不会把那个已修复的缺陷重新引入。

为此把三处差异抽成**类属性**（父类默认值保持不变，向后兼容）：

| 类属性 | `OcrTextDialog`（图级） | `DeclarationTextDialog`（记录级） |
|---|---|---|
| `_WINDOW_TITLE` | `原始 OCR 文本` | `申报要素原文` |
| `_HINT_TEXT` | 可边看图边对照 | 正文为整段原文（未经解析清洗，原样展示） |
| `_NAV_VISIBLE` | `True`（带「上一张 / 下一张」） | `False`（申报要素与当前图无关，翻图无意义） |

另新增 `OcrTextDialog.set_hint()`，供子类挂**附加提示**而不污染正文。

⚠️ **实现陷阱（本轮踩到）**：仅"把按钮创建出来但不加进布局"**不足以**让它
`isVisible()` 为 `False` —— 父窗口 `show()` 时会连带显示从未被**显式隐藏**的子控件。
必须在 `_build_ui` 里显式 `self.btn_prev.hide()`（回归锁断言
`btn_prev.isVisible() is False`）。

### 内容红线（本版最需要守住的一条）

弹窗正文 = `record.raw_element_text` **逐字符原样**：不做 `strip` / 不清洗 / 不截断 /
**不反解析**。这是「原始输入只进不改」铁律，也是判定契约的要求
（`docs/04` 裁决二：`raw_element_text` 必须保留原始形态，清洗会吞掉需要被报告的差异字符）。
回归锁 `test_view_decl_dialog_shows_raw_element_text_verbatim` 用**带首尾空白 + 多行**的
样本断言 `current_text() == raw`（**不是** `in`），任何 `strip` 都会被红灯逮住。

解析出的品牌 / 型号只走 `set_hint()` 作为**附加提示**（"解析结果（仅供参考，正文未改动）"），
与原文**并列**展示，便于复核人一眼看到"工具从这段原文里取到了什么"。

### 行为细节

| 场景 | 行为 |
|---|---|
| 两个弹窗同时打开 | **互斥** —— 两窗共用右侧同一定位会**完全重叠**，故开一个即关另一个 |
| 切换当前图 | 申报要素内容**不变**（记录级），OCR 弹窗内容**跟随**（图级） |
| 切换记录（弹窗开着） | 弹窗内容**自动同步**为新闻的原文，不留旧值 |
| 卡片 `clear()` | 两个弹窗一并隐藏 |
| 未载入记录时点击 | 不弹窗（避免展示空壳） |
| 记录无 `raw_element_text` | 显示明确占位语「（本条记录无申报要素原文）」，而非空白窗口 |
| 标题 | `第 N 条 · 料号 … · 订单号 … · 票号 …`（缺字段跳过）—— 便于复核留痕 |

### 工具 · 视觉验收扩展

`tools/ui_preview.py` 由「1 张或 2 张 PNG」改为**最多 3 张**：新增
`preview_workbench_with_decl_dialog.png`（申报要素弹窗叠加图）。
两个弹窗**各自铺干净画布单独渲染**（因为互斥）。运行结果：
两个弹窗几何自检**各 4/4 OK**（右对齐 ±1 / 垂直居中 ±1 / 不遮图片区 / 完整落在屏内）。

### 验证

| 项 | 结果 |
|---|---|
| 全量测试 | **1174 passed / 0 failed / 0 errors**（v0.3.2 = 1162，净增 12） |
| `ruff check .` | All checks passed |
| 新增用例 | `tests/test_ui_workbench_d_class.py` 增 12 条：按钮**相邻**关系、正文逐字符原样、标题纯函数（含空记录容错）、无翻图导航、切图不变 / 换记录同步、两窗互斥、**不遮图片区**（`overlap_with` 硬断言）、`clear()` 联动、无记录不弹窗、无原文占位语、解析提示语 |
| 视觉验收 | 3 张 PNG；两弹窗几何自检各 **4/4 OK**；工作台截图中「查看申报要素」按钮就位于 OCR 按钮右侧 |
| 口径漂移 | **零** —— `core/`（含 `judge_engine` / `noise_guard` / 8 件 YAML）**本轮零改动**；真实样本基线未重跑（无口径变更，无需重设） |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = `build_exe.ps1` = **0.3.3** |

### 打包与冻结态交付核对（v0.3.3）

> 命令：`.\build_exe.ps1 -Zip -Python <健康 venv>`
> ⚠️ **打包前必须先清空 `PYTHONPATH`**（否则 safe-delete 拦截件随 `sitecustomize.py` 自动加载，
> 见 v0.3.1「沙箱陷阱」）。

| 项 | 结果 |
|---|---|
| 打包前置体检 | `tools/check_env.py` **exit 0**（19 关键依赖 + 23 模块 import + 无空壳包） |
| 打包耗时 | **1 分 30 秒** —— 比 v0.3.1 的 ~3 分钟快，因为 `dist\报关申报要素校验工具\` **已不存在**（只剩 3 个历史 zip），COLLECT 无需清理 344 MB 目录 → **未触发 `trash-failed` fail-closed** |
| onedir 产物 | `dist\报关申报要素校验工具\` —— **721 文件 / 361,422,216 字节（344.7 MB）**；exe 本体 10,374,511 字节 |
| 分发包 | `dist\报关申报要素校验工具_v0.3.3_0917.zip` —— **157,651,969 字节（150.3 MB）** |
| zip sha256 | `82ecce181935893a52324d48d88d3f37954de6b9d52c4c4cd323e9cde865231c` |
| zip 结构 | 731 条目 / 顶层仅 `报关申报要素校验工具` / 根下 exe 就位 / `testzip` 通过 |
| 冻结态 `--self-test` | **exit 0，0.51s**（日志确认 `报关申报要素自动校验工具 v0.3.3 启动`） |
| 冻结态 `--check-env` | **exit 0，0.88s** |
| `_internal\rules\` | **8/8 YAML** 收全 |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = zip 文件名 = **0.3.3** |
| **新口径确实随包出厂** | `_internal\rules\brand_patterns.yaml` 含 `none_markers:`；`noise_signals.yaml` 含 `letter_only_difference`（min_length 4 / max_edit_distance 2 / SUSPICIOUS）；两文件**源码 ↔ 冻结态 sha256 完全相同** |
| **新代码确实进 PYZ** | `PYZ-00.toc` 含 `core.none_marker` / `core.judge_engine` / `core.noise_guard` / `ui.widgets.ocr_text_dialog` / `ui.widgets.workbench_card` —— ⚠️ **只能查 TOC，不可在 exe 字节里搜模块名**（纯 Python 模块在 PYZ 归档内） |

> ⚠️ **仍未闭环**：现场 **Win10 1809+ / Win11 x64 干净机双击 exe 真机验收** ——
> 本项目**唯一**未闭环的交付验收项（命令行冒烟只覆盖 `--self-test` / `--check-env`，
> GUI 路径「窗口能开 + 跑批能出表」必须人工走一遍）。

> 📌 **本版未提交**（用户明确要求先打包不提交）：工作树就地保留 21 改 4 新增。

## [0.3.2] · 2026-09-17（判定口径三项变更 —— **用户裁定 2026-09-17**）

> ⚠️ **口径级变更**：三项均触及判定结论，按项目红线「改口径须写 CHANGELOG + 重设基线」执行。
> 三项口径在动手前经 `AskUserQuestion` 与用户逐项确认（用户均选「推荐」方案），
> 确认记录见下表「裁定」列。**这不是"改测试凑绿"，是用户裁定的口径演进。**

### 变更 1 / 2 · 显式「无品牌 / 无型号」标记 → 该要素核验通过

**用户原话**：申报要素中品牌为「无」或没有明确品牌（没有该要素说明），而图片 OCR 中
出现「无品牌」字样 → 品牌核验通过；型号同理。

| # | 争议点 | 裁定 |
|---|---|---|
| 1 | 「无」标记与其他品牌文字并存时谁说了算 | **以「无」标记为准 → 通过**（显式标记是标签本体直接证据，优先级高于其他位置的品牌文字）；其他品牌文字仍写入差异备注供人工追溯 |
| 2 | 认定范围 | **字段行或独立行的显式标记**（如 `品牌:无` / `品牌：无品牌` / `Brand:NONE` / 独立一行 `无品牌`） |

**实现**：

- 新建 `core/none_marker.py`：`NoneMarkerHit`（frozen dataclass，可 `to_dict()`）+ 唯一取证入口
  `detect_none_markers(texts, *, rules)`（逐图逐行，每字段只保留首个命中，顺序确定）
  / 单要素便捷入口 `detect_none_marker()`；`_resolve_image_seq()` 在 `OcrText.seq == 0` 时
  按文件名 `&NNN` 兜底解析图号。
- 规则**外置**（补规则 = 改 YAML 不改代码）：`rules/brand_patterns.yaml` 新增 `none_markers` 节点
  （4 条：`brand_labeled` / `brand_bare` / `model_labeled` / `model_bare`），
  兜底常量 `core/constants.py::DEFAULT_NONE_MARKERS`（**兜底 ≡ repo**，见下方「缺陷 F 同类隐患」）。
- `core/token_matcher.py`：`TokenMatch` 新增 `none_marker` / `none_marker_image`（只进详细 JSON + UI，
  **不进 13 列汇总表**）。
- `core/judge_engine.py`：新增 `_annotate_none_marker()` —— **只在 `is_none_token(declared)` 时写入**留痕
  （`mode` 保持 `NONE`，不改写取证结论，**诚信取证**）；`_field_state_v03` 的「申报为无」分支改为
  「有显式标记 → `BOTH_ABSENT`（通过）／无标记 → 走原逻辑」；PASS 理由追加标记说明。
- `ui/widgets/workbench_card.py`：判定列在 `NONE` 分支显示 `✅ 图内显式标注「无品牌」（图 N）`。

**防虚高护栏（反向锁，共 14 条）**：`品牌:无锡机电`（地名开头）、`无瑕科技`、`无印良品`、
`品牌类型:0`（v0.3.1 收窄口径）等**均不得**误判为「无」。正则以
`(?=$|[\s,，。;；、|/\\])` 收尾断言右边界，且**不带捕获组**（防误取组值）。

### 变更 3 · 纯英文「仅字母之差」→ 待复核 ⚠️（并写明复核依据）

**用户原话**：申报值与 OCR 识别值若都是英文、字母数超过 3，且**只有字母之差** →
列为待复核，并说明复核结果。

| # | 争议点 | 裁定 |
|---|---|---|
| 3 | 「只有字母之差」的尺度 | **双方均纯英文 + 长度均 > 3 + 编辑距离 ≤ 2** 才降级为待复核 |

**实现**：

- 规则外置 `rules/noise_signals.yaml` 新增 `letter_only_difference` 节点：
  `min_length: 4` / `max_edit_distance: 2` / `verdict: SUSPICIOUS`；
  对应常量 `DEFAULT_LETTER_DIFF_MIN_LENGTH` / `DEFAULT_LETTER_DIFF_MAX_EDIT_DISTANCE` /
  `DEFAULT_LETTER_DIFF_VERDICT`。
- `core/noise_guard.py`：`classify_detail()` 在既有 ④ `fuzzy_similarity` **之后**插入 **④′** 分支
  （`signals=["letter_only_difference"]`）；新增 `_is_ascii_alpha()` 与 `_is_letter_only_difference()`。
- ⚠️ **陷阱**：`str.isalpha()` 对**中文同样返回 True** —— 判「纯英文」必须显式判
  `"A" <= ch.upper() <= "Z"`，否则 `品牌` 与 `型号` 会被当成"只有字母之差"。
- **红线守护**：本分支强制 `SUSPICIOUS` → 结论最多 ⚠️，**绝不直达 ❌**（不虚高）。
- 复核说明随 `reason` / `differences` 输出，写明「仅字母之差（编辑距离 N）」，供人工比对。

### 修复 · 缺陷 F 同类隐患（兜底分支 ≠ repo 规则集）

`core/noise_guard.py::_resolve_noise_rules()` 兜底分支原**只补了部分字段**：本轮新增的
`letter_diff_*` 与既有的 `low_confidence_threshold` / `fragment_min_chars` / `known_noise_samples`
在缺 YAML 时会静默取默认值 → **源码态全绿、打包漏 YAML 后判定全变**（缺陷 F 的翻版）。

- 兜底分支**逐字段补全**，机器锁 `tests/test_noise_guard.py::TestFallbackEquivalence`
  （多组抽样断言 `guard_no_rules.classify(...) == guard.classify(...)`）。
- ⚠️ 同时**修正**了 `TestFuzzyThresholds::test_no_rules_fallback` —— 该用例此前实际锁死了
  「兜底漏传 `known_noise_samples`」的**错误行为**（兜底 `SUSPICIOUS` ≠ repo `DEFINITE_MATCH`）。
  这是**口径等价性修正，非改测试凑绿**。

### 验证

| 项 | 结果 |
|---|---|
| 全量测试 | **1162 passed / 0 failed / 0 errors**（v0.3.1 = 1069，净增 93） |
| `ruff check .` | All checks passed |
| 新增用例 | `test_none_marker.py`（44：19 种书写形态 + 14 条防虚高反向锁 + 规则来源 + 单要素入口）／`test_judge_none_marker.py`（需求 1/2 端到端 + 优先级 + 13 列冻结）／`test_noise_guard_letter_diff.py`（规则级边界 + 端到端 + 规则外置）／`test_rule_repository.py` / `test_constants.py` / `test_noise_guard.py` 增量 |
| 真实样本基线 | SA26090215（18 条）**离线复跑零漂移**：✅7 / ❌7 / ⚠️4 / 🔵0 —— 与 v0.3.0 基线**逐条相同**（本轮三条口径均未在该样本上触发变更） |
| 13 列汇总表 | **未动**（`test_token_matches_not_in_summary_row` 硬锁） |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = `build_exe.ps1` = **0.3.2** |

> 📌 **本轮未打包**：这是**源码级口径交付**。exe 需用户在真实样本上复看新口径效果后再决定是否出包
> （打包前**必须先清空 `PYTHONPATH`**，见 v0.3.1「沙箱陷阱」）。

## [0.3.1] · 2026-09-16（现场反馈：2 处 UI 缺陷 + 1 项口径收窄）

> 触发：现场截图 2 张 + 用户口头补一条口径。**默认项目 0.3.0（`4ad328b`）之上增量修复**。
> 基线口径**零漂移**：`core/judge_engine.py` / `core/noise_guard.py` **本轮零改动**。

### 修复 1 · 判定链路表格「错位覆盖」（现场截图 1）

现象：判定链路表里「判定原因」文本**压在表格「型号」行上**，表格只剩「品牌」一行可见。

根因**不在表格自身**（表格几何一直是正确的：`height=113 = 表头 29 + 2 行 ×40 + 边框`），
而在**父容器把子控件压到最小尺寸以下**：

| 层 | 实测 | 说明 |
|---|---|---|
| 卡片内容 | 需 886px，实操只拿到 **759px**（= 视口高） | `QScrollArea(widgetResizable=True)` 只把内容拉到 `max(视口高, 内容最小高)`，而内容布局的最小高只有 600 |
| 判定链路框 | 被压成 **190px** < 其 `minimumSizeHint` **194px** | 缺的 4px 正是重叠的起点 |
| 「判定原因」 | `y=109`，而表格占 `30..143` | 文字溢出自身矩形 → 视觉上压住「型号」行 |

而内容之所以撑不高，还有**第二层根因**：`QLayout.addWidget()` 内部用
**queued** `QMetaObject::invokeMethod(w, "_q_showIfNotHidden")` 显示新子控件 ——
`load_result` 里刚 new 出来的散行标签在事件循环下一拍前仍是 `isHidden()`，
`FlowLayout.sizeHint()` / `heightForWidth()` 一律返回 **0**，于是「内容需要多高」
被测成 0 / 577 / 718 之类的小值（稳定值其实是 886）。

修法（四件套，缺一不可）：

| # | 修法 | 位置 |
|---|---|---|
| 1 | 整卡内容套 `QScrollArea`（横向不滚动，纵向按内容） | `ui/widgets/workbench_card.py::_build_ui` |
| 2 | `_fit_content_height()`：用 **`heightForWidth(视口宽)`**（非 `sizeHint`，后者按"不换行"估算成 1274，会让卡片白滚）写回内容 `minimumHeight` | 同上 |
| 3 | `_reveal()`：新增流式子控件（散行标签 / 图号按钮）**显式 `show()`**，让几何查询当拍即准 —— 这是本次最关键的一条 | 同上 + `_fill_flow_labels` / `_render_evidence_buttons` |
| 4 | 表头**左对齐**（原居中，与单元格默认左对齐不一致 → 表头文字与列内容看着错开）、关闭双滚动条、前两列 `Fixed` + 第三列 `Stretch`、`_fit_chain_height()` 按**表头实际高**校正 | `_build_chain` / `_fit_chain_height` |

附带**新发现并一并修掉**的问题：内容恢复真实高度后，「重判为 / 备注 / 标记待补图 /
保存重判」被推到折叠线以下 —— 等于把"压扁"换成"藏起来"。故底部操作区移出滚动区
（`QFrame#cardFooter`，常驻），`shell = 滚动区(拉伸 1) + 操作区(0)`。

**可证伪性验证**（同一探针，仅把 `_reveal` 打回空操作）：

| 观测量 | 修复态 | 回退态 |
|---|---|---|
| 链路框高 | **228** ≥ 194 ✓ | **190** < 194 ✗ |
| 「判定原因」y | **147**（表格底 143） | **109**（压在表格上） |
| 几何重叠 | **False** | **True** |
| 内容最小高 | **886**（稳定） | 718 / 759（乱跳） |

### 修复 2 · OCR 弹窗「置顶」改为右侧垂直居中（现场截图 2）

现象：弹窗出现在窗口**左上角**，压住图片区与右侧证据卡。

修法：把定位逻辑抽成**纯函数** `OcrTextDialog.place(area, win_geo, anchor) -> QRect`
（可任意分辨率单测，不再依赖真实窗口），优先级：
① 图片区右侧够放（≥ `_MIN_WIDTH` 360）→ **右对齐窗口右边界 + 垂直居中**（按需收窄）；
② 右侧放不下 → **贴图片区下方**（保住"不遮图"这条硬约束）；
③ 上下左右皆无空间 → 贴右侧 + 垂直居中 + 夹回屏内。删除失效的 `_fits()`。

### 变更 · 判定范围收窄为「只识别品牌与型号」（用户口径 2026-09-16）

新增两条常量并落护栏（`core/constants.py`）：

- `JUDGED_FIELDS = (FIELD_BRAND, FIELD_MODEL)` —— 参与识别与判定的**仅此两者**；
- `NON_VALUE_FIELD_SUFFIXES = ("类型", "种类", "类别")` —— 「品牌类型 / 型号类型」的取值是
  **品牌的归类**而非品牌本身。⚠️ 缺此护栏时 `品牌类型:0` 会被读成品牌值 `0`，**违反「不虚高」红线**。

`core/element_parser.py` 三处候选收窄（`_is_brand_key` / `_is_model_key` / `_extract_*` 分支）
均加 `_is_non_value_key()` 排除。

> ⚠️ **这是防御性加固，不是行为变更**：实测申报要素原文
> `用途:电视机用|结构类型:有接头|品牌:baori|型号:无|额定电压:60V` ——
> 程序本来就只判品牌 / 型号。本轮重点是**堵住"品牌类型被当品牌值"的虚高风险**。

### 验证

| 项 | 结果 |
|---|---|
| 全量测试 | **1069 passed / 0 failed / 0 errors**（v0.3.0 为 1026，净增 43；17.81s） |
| `ruff check .` | All checks passed |
| 新增回归锁 | 弹窗定位 15 条（`tests/test_ocr_dialog_position.py`，含**对旧缺陷的反向锁** `rect.top() > 8`）；链路框不压缩 / 不重叠 / `heightForWidth` 幂等 / 流式标签当拍 show 共 4 条；表头左对齐等 4 条；判定范围 15 条 |
| 视觉验收 | `tools/ui_preview.py`（离屏 + 模拟 1920×1080 屏 / 1600×900 窗）产出 2 张 PNG；弹窗几何自检 **4/4 OK**（右对齐 ±1 / 垂直居中 ±1 / 不遮图片区 / 完整落在屏内） |
| 口径漂移 | `core/judge_engine.py` / `core/noise_guard.py` **零改动**；四类判定字符串未动（`tests/test_constants.py` 硬断言仍绿） |

### 打包与冻结态冒烟（v0.3.1）

> 命令：`.\build_exe.ps1 -Zip`（onedir + zip）。⚠️ **打包前必须先清空 `PYTHONPATH`**，见下「沙箱陷阱」。

| 项 | 结果 |
|---|---|
| onedir 产物 | `dist\报关申报要素校验工具\` —— **721 文件 / 361,401,898 字节（344.7 MB）**；exe 本体 10,357,659 字节 |
| 分发包 | `dist\报关申报要素校验工具_v0.3.1_0917.zip` —— **157,633,359 字节（150.3 MB）** |
| zip sha256 | `1e8e4b2036d0fd754136275a3a36390668ddd206b7b0d37a2a4cb4ef23ce5307` |
| zip 结构 | 731 条目 / 顶层仅 `报关申报要素校验工具` / 根下 exe 就位 / `testzip` 通过 |
| 冻结态 `--self-test` | **exit 0，0.51s**（日志确认 `报关申报要素自动校验工具 v0.3.1 启动`） |
| 冻结态 `--check-env` | **exit 0，0.92s** |
| `_internal\rules\` | **8/8 YAML** 收全 |
| 版本号四处一致 | `main.py` = `ui/main_window.py` = `pyproject.toml` = zip 文件名 = **0.3.1** |
| 打包前置体检 | `tools/check_env.py` exit 0（19 依赖 + 23 模块 import + 无空壳包） |

⚠️ **沙箱陷阱（本轮新增第 4 类，务必记录）**：首次打包在**最后一步 COLLECT** 失败 ——

```
INFO: Removing dir ...\dist\报关申报要素校验工具
[safe-delete][SAFE_DELETE_FAIL_CLOSED] {"reason": "trash-failed",
 "detail": "SHFileOperationW 失败: 0x78"}
OSError: SHFileOperationW 失败: 0x78      → PyInstaller exit 1
```

根因：沙箱把删除拦截件装成 **`sitecustomize.py`**，而它是靠 **`PYTHONPATH` 指向
`…\cli\vendor\shim`** 才被 Python 自动加载的（PyInstaller 日志的 "Module search paths
(PYTHONPATH)" 里能直接看到这一条）。该拦截件走「移到回收站」路线，对 **3000+ 个文件、
344 MB、含中文与超长路径**的目录会失败并 **fail-closed**（宁可报错也不真删）。

**绕行：打包前清空 `PYTHONPATH`**（shim 就不会被加载，删除回到系统原生语义）：

```powershell
Remove-Item Env:PYTHONPATH -ErrorAction SilentlyContinue
```

清空后同一条命令**一次通过**（Analysis→PYZ→PKG→EXE→COLLECT→zip 全绿）。
抬高 `CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD` 对**本类失败无效**（它走的是
`trash-failed` 分支而非批量阈值分支）。

📌 **另一个包装卫生点**：冻结态冒烟会在 onedir **旁边**生成运行目录
`dist\报关申报要素校验工具\报关申报要素校验\`（v0.2.0 起的运行目录约定）。
它与 zip 无关（STEP 6 先压缩、后冒烟），但**若之后再次压缩会把运行日志带进包**
→ 冒烟后需清理，或直接重新打包（PyInstaller 会整目录重建）。

📌 **冒烟已固化为常备工具**：本轮起初用的是一次性脚本 `_smoke.py`（版本号硬编码，
核对完即删），现已提升为 **`tools/smoke_frozen.py`** —— 版本 / 包名 / zip 全部从工程
现状**自动推导**，以后每次打包直接跑，换版本不用改脚本：

```powershell
Remove-Item Env:PYTHONPATH -ErrorAction SilentlyContinue
python tools/smoke_frozen.py --out _smoke.txt      # --no-run 可跳过 exe 冒烟
```

六项检查（任一**致命项**不过即 `exit 1`）：

1. onedir 规模（文件数 / 字节 / exe 字节）+ 检出混进包里的运行目录；
2. zip：精确字节、sha256、`testzip` 完整性、条目数、**根下 exe 是否就位**；
3. 冒烟 `--self-test` / `--check-env` 的**退出码 + 耗时**，**带 300s 超时**
   —— 打包态弹模态框会永久挂起，必须靠超时兜底，不能只等；
4. `_internal/rules/` 与源码 `rules/` **逐个文件名比对**（能报出缺哪几个，
   不只数个数 —— 缺陷 F 的教训）；
5. 版本号**四处**一致：`main.py` / `ui/main_window.py` / `pyproject.toml` / zip 文件名；
6. **冻结态实际运行版本**：从 `--self-test` 的运行日志里断言「… v0.3.1 启动」。

⚠️ 第 6 项**不要**写成「在 exe 字节里搜版本串」：纯 Python 模块被 PyInstaller 放进
**PYZ 归档**（压缩），`_MEIPASS` 只落地二进制与数据 —— 搜不到是**正常**的，那样写会造出
稳定复现的**假报警**（初版即踩，返回 `含 0.3.1：False`）。权威判据是运行日志那一行。
同理，冒烟日志走 **stderr**（`logging` 默认流），别因为"stderr 有内容"就判失败。

### 已知限制

- 卡片内容超过窗格高时**纵向滚动** —— 这是本次的**设计取向**（宁可滚动，不可压扁）。
  ⭐ **用户 2026-09-17 00:08 明确确认**：采用「整卡纵向滚动 + 底部操作区常驻」，
  **不**改为「只让散行 OCR 区滚动」。理由：复核卡各段都可能超高（判定原因长文案、
  证据图多、散行多），局部滚动会让"哪一块在滚"变得不可预期；整卡滚动心智一致，
  且操作区已固定，保存动作无需滚动。
- 离屏渲染不加载 Windows 系统字体，需显式 `addApplicationFont("C:/Windows/Fonts/msyh.ttc")`，
  否则中文全渲染为豆腐块（`tools/ui_preview.py::_install_cjk_font` 已处理）。

## [0.3.0] · 2026-09-16（批次 1：判定主链路口径变更）

> ⚠️ **本项目首次改动 `core/judge_engine.py`**（v0.1.0 / v0.2.0 两轮它均为零改动）。
> 依据：`docs/10_迭代方案_v0.3_0916.md`（需求 1）+ 用户 2026-09-16 四项口径拍板。
> 状态：**批次 1 完成**；批次 2（复核工作台重构）/ 批次 3（打包推送）待做。

### 变更（口径变更 —— 需显式记录）

| 项 | 变更前（v0.2.0） | 变更后（v0.3.0） |
|---|---|---|
| 图片侧取证 | **先提取、后比对**：`extract_detected_brand/model` 用四层正则 + 跨图投票**猜出**图片侧值，再交 `NoiseGuard.classify_detail` 比对 | **完整分词直接命中**：申报值在 OCR 全文里以完整分词出现即算命中（`core/token_matcher.py`） |
| 根因 | OCR **缺失空间感知**（无左右/上下关系）→ 字段名与值分行、值被相邻字段名抢走 → 猜错或猜空 → 误判 | 取证不再依赖"猜值"，空间关系缺失不再是失效路径 |
| 未命中口径 | 按 `NoiseGuard` 三态 | **两级分流**：图内**有同类标识**但值不同 → ❌（有证据）；图内**根本没有**该类标识 → ⚠️（没找到证据） |
| `extract_detected_*` 职能 | 取证主路径 | **降级**为「图内是否存在同类标识」判据 + 13 列展示值 + 整机品牌定位键 |

**明确不变**（防止误读为口径漂移）：
图片证据闸门（🔵）、整机品牌保留 ❌（T06 裁决 A′）、记录级跨文字体系兜底、
`NoiseGuard` 比对语义与 `SUSPICIOUS` 降级护栏、四类口径字符串、`COLUMN_COUNT = 13`
与汇总表列结构、OCR 并发/缓存/降采样。

### 新增（功能）

- **`core/token_matcher.py`**：`TokenMatcher` / `TokenMatch` + 三级匹配
  （`EXACT` 英文数字按词边界、中文按子串 → `FUZZY` 已知误读纠正 + 易混字符等价类 → `NONE`）。
- **字段名上下文护栏（防新链路引入虚高）**：中文子串匹配会把**字段名**当值
  （申报 `创维` × 图上 `创维物料编号`）→ 命中作废。三道判据：G1-CJK 紧邻后缀、
  G1-ASCII 词边界后缀、G2 整串命中 `fields_blacklist` 词表；
  **同源规则**：已知误读样本的纠正目标本身若是字段名（`SKYWORTH P/N`），该样本不作命中依据。
- **`CheckResult.token_matches`**：品牌/型号各一条命中取证（`mode/token/images/sample_line/corrected_from/note`），
  **只进详细 JSON + 复核工作台，绝不进 13 列汇总表**（`to_row()` 零改动）。
- **`tools/eval_v03.py`**：新旧口径逐条对比评估器（旧引擎从 git `dfe00d5` 取源码独立加载，
  证据取真跑批快照、申报侧用当前解析器刷新 → 严格 A/B）。

### 口径变更实测（SA26090215，18 条）

```
快照记录（v0.2.0）  ：✅1 / ❌12 / ⚠️5 / 🔵0
旧链路复算（自证）  ：✅1 / ❌12 / ⚠️5 / 🔵0   ← 与快照**完全一致**，证明对照未走样
新链路（v0.3.0）    ：✅7 / ❌7  / ⚠️4 / 🔵0   ← 新回归基线
口径变更：6 / 18 条（全部 ❌/⚠️ → ✅）
```

| seq | 旧 → 新 | 申报 | 命中取证 |
|---|---|---|---|
| 1 | ❌ → ✅ | `baori` | 图 2 实物印字 `baori` |
| 4 | ⚠️ → ✅ | `宇同` | 图 1 `宇同电子（惠州）有限公司` |
| 5 | ❌ → ✅ | `baori` | 图 4 `baori E339609 AWM 20941 …` |
| 16 | ❌ → ✅ | `DAEWOO` / `HS-8A50J-12` | 图 3 唛头 `DAEWOO`；图 6 标贴 `HS-8A50J-12 K1 …` |
| 17 | ❌ → ✅ | 同上 | 图 3 / 图 4 |
| 18 | ❌ → ✅ | 同上 | 图 2、4 / 图 3 |

**未变**：整机品牌类 seq 9/11/12/13/14/15 **全部保留 ❌**（R2 回归锁）；🟡 seq 3/6/7/10 保留 ⚠️；
🔵 仍为 0。**护栏在真实样本上零误伤**（作废次数 0）。

> ⚠️ **seq 16/17/18 的双重根因**：其旧 ❌ 既来自新链路要解决的"猜值错误"
> （旧链路取到 `SKYHORTH` / `HS-8AA` 这类字段名残片与截断值），也来自快照的
> **申报侧陈旧**（`decl_model` 为空，实为缺陷 E 修复前遗留）。本次已一并修正
> 回归夹具：`tests/test_real_sample_sa26090215.py` 改为**用当前解析器刷新申报侧**。

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **991 用例 / 0 失败 / 0 错误**（v0.2.0 为 929；新增 `test_token_matcher.py` 47 条 + 真实样本层 5/基线锁 15 条） |
| 静态检查 | `ruff check .` All checks passed |
| 真实样本 | ✅7 / ❌7 / ⚠️4 / 🔵0（`test_v030_baseline_locked` 硬断言） |
| 不虚高红线 | 全样本 `SUSPICIOUS` 记录**无一**判 ❌（`test_suspicious_never_escalated_to_fail`） |
| 13 列冻结 | `to_row()` 零改动，列数硬断言通过 |
| 架构守卫 | `core/` 与 `app/` 仍未 import PySide6 |

### 已知影响 / 待裁决

1. ⚠️ **「整机品牌 → ❌」与「命中即合格」的交叉场景**：seq=1 既有整机命中
   （`Brand:Daewoo` + `CARTON`）又有本体命中（图 2 印字 `baori`），新链路判 ✅。
   本实现取「申报值确在图中 → 合格」（整机品牌仍**不被当成本体证据**，未违反 A′ 的实体语义）。
   如认为"整机命中即须强制 ❌"，请裁决 —— 该分支改动量极小。
2. v0.4 候选：管线产出的 `OcrText.seq` **恒为 0**（序号只在 `ImageEvidence.seq` 上），
   本次在 `token_matcher` 内按文件名兜底解析；建议在管线侧补齐该字段。
3. 快照 `过程产出/校验详细日志_SA26090215_累积.json` 的申报侧已陈旧（见上），
   后续跑批请重生成累积 JSON。


---

## [0.3.0] · 2026-09-16（批次 2：复核工作台重构 + 批次 3 收尾）

> 依据：`docs/10_迭代方案_v0.3_0916.md` 需求 2/3/4/5/7/8；
> 承接批次 1（判定主链路口径变更）——本轮**纯 UI + 收尾**，判定引擎零改动。

### 新增（功能）

| # | 需求 | 落地 |
|---|---|---|
| 2 | **图片旋转** | `ui/widgets/image_viewer.py`：`↺ 左旋` / `↻ 右旋` 90°、`rotation_label` 显示角度；`display_pixmap()` 作为**旋转后**尺寸基准（`_render` / `fit_to_window` / `is_pannable` 统一改读），旋转后自动 `fit_to_window()`；换图 / 清空自动复位 0° |
| 3 | **状态标签 + 模糊搜索** | `ui/widgets/review_workbench.py`：5 个状态标签（全部 / 成功 / 待复核 / 缺图 / 失败）**带数量与灯色**，数量取全量统计（不随过滤变动）；搜索框对 `出货单号 / 订单号 / 物料编号` 三字段做**大小写不敏感子串**匹配；过滤管线 `全部 → 状态标签 → 搜索` |
| 4 | **记录改表格 + 选中同步** | 左列 `QListWidget` → `QTableWidget`（`订单号 / 物料编号 / 核验结果`），`SelectRows + SingleSelection`，选中行整行高亮；选中变化 `_sync_detail(row)` 同步中间图片与右侧卡片 |
| 5 | **判定链路三列** | `ui/widgets/workbench_card.py`：4 段 `QLabel` → **3 列 × 2 行** 表（`要素 / 申报值 / 判定值`）；判定值取**引擎回吐**的 `TokenMatch`（EXACT / FUZZY / NONE 三分支 + 字段名护栏作废说明 + 旧结果集回退并标注「旧链路」），单元格 tooltip 载明命中方式 / token / 图号 / 原文行 / 说明 |
| 7 | **全部改散行文本** | 删除 KV 表格（`kv_table` / `_kv_rows()` / `_line_key()` / `_KV_TABLE_QSS`）；散行卡成为唯一展示形态，数据源改 `ocr.lines()` **全量行**（不再剔除已成键值对的行）。`OcrText.kv` **仍提取、仍落详细 JSON**（Q4 口径不变），仅不再进 UI |
| 8 | **只留原始 OCR + 弹窗不遮图** | 删除 `evidence_view`（与原始 OCR 重复）与 `raw_toggle` / `raw_view`（内嵌折叠区）→ 新增 `ui/widgets/ocr_text_dialog.py::OcrTextDialog`：**非模态单例**弹窗、`◀ 上一张 / 下一张 ▶ / 复制全文`、随切图自动刷新 |

### 新增（工程）

- `tools/run_sample.py`：**headless 真实样本端到端跑批**（与 GUI 同走 `CheckTask`），
  打印四类分布 / 三产物落点 / 耗时，并自动比对 `原始输入` 跑批前后指纹。
  支持 `--check-input`（只算指纹）、`--json`（落分布 JSON）、`--base`（覆盖输出锚点）。
- `core/constants.py` 新增 `FIELD_BRAND` / `FIELD_MODEL` 作为字段名**唯一来源**；
  `core/judge_engine._FIELD_*` 改为其别名，由 `test_constants.py::TestFieldNames` 锁一致。

### 修复（本轮实测发现的 4 处）

| # | 问题 | 根因与修法 |
|---|---|---|
| 1 | 弹窗避让恒为「不生效」 | `WorkbenchCard` 被 `QSplitter.addWidget()` **重新挂载**，`self.parent()` 是 splitter 而非工作台 → `_image_area()` 改为**向上遍历父链**（限深 8 层）找 `image_viewer`。缺此修复弹窗会压住图片（违反需求 8 红线） |
| 2 | 「核验结果」列被挤成 `…` | 三列并排时左栏 260px 不足以放下 17 字料号 + 结果列 → 左栏 `minimumWidth` 400、右卡 460、分栏比 `2:3:4`、表头 `minimumSectionSize=72` |
| 3 | 判定链路表下方一大片空白 | `QTableWidget` 默认 `sizeHint` 高 192px → `setFixedHeight(28 + 2×40 + 4)` + 行高 40 + `setWordWrap(True)` |
| 4 | 深色面板落在**浅色**主题上（形似渲染异常） | v0.2.0 的散行卡 / KV 表用「深色面 + 浅色字」，代码注释理由是"适配本机深色主题"，但 `ui/styles/app.qss` 实际是**浅色主题**（`#F5F6F8` 底 / `#303133` 字）→ 统一改浅色；判定徽标亦由枚举名 `PASS` 改为口径字符串 `✅ 校验合格` |

### 行为变更（需知悉）

| 项 | v0.2.0 | v0.3.0 | 理由 |
|---|---|---|---|
| 复核工作台**默认列出的记录** | 仅 ⚠️ + 🔵（待复核） | **全部**四类 | 「谁要复核」由状态标签的数量 + 头部「待复核：N 条」承载；避免 ❌ 等问题记录被静默隐藏。`tests/test_ui_smoke.py` 对应用例同步改名改断言（口径变更，非改测试凑绿） |
| 散行卡 / 判定链路表配色 | 深色面 + 浅色字 | 浅色面 + 深色字 | 与 `app.qss` 浅色主题一致（见上表 #4） |

### 验证

| 项 | 结果 |
|---|---|
| 回归测试 | **1026 用例 / 0 失败 / 0 错误**（批次 1 为 991；本轮净增 35 条 UI 用例） |
| 静态检查 | `ruff check .` All checks passed |
| 离屏视觉验收 | 复核工作台**整屏渲染截图**逐项核对 6 条需求；弹窗 `overlap_with(图片区) == False` 硬断言 |
| 架构守卫 | `core/` 与 `app/` 仍未 import PySide6；`COLUMN_COUNT = 13` 与四类口径字符串零改动 |
| 判定引擎 | `core/judge_engine.py` / `core/noise_guard.py` 的**判定语义**本轮未动 |

### 批次 3：真实样本端到端复跑（T13）

用新增的 `tools/run_sample.py` 对真实样本 SA26090215 全量跑批（headless，与 GUI 同走
`CheckTask`）：

| 项 | 结果 |
|---|---|
| 结果 | `ok=True`，**18/18 条**处理完成 |
| **四类分布** | **✅7 / ❌7 / ⚠️4 / 🔵0** —— 与批次 1 确立的新基线**完全一致**（口径未漂移） |
| 耗时 | **181.2s**（v0.2.0 为 218.9s；OCR 缓存命中 0 / 实际识别 113 张，无损坏） |
| 三产物 | `result/校验汇总表_SA26090215.xlsx`（**19 行 × 13 列**，列名与 `COLUMNS` 逐字一致）／`result/SA26090215_待人工复核清单.csv`／`logs/校验详细日志_SA26090215_累积.json`（18 条，含 `token_matches`，不含 `boxes`） |
| 逐条 | idx1 ✅ ／ idx2 ❌ ／ idx3 ⚠️ ／ idx4 ✅ ／ idx5 ✅ ／ idx6 ⚠️ ／ idx7 ⚠️ ／ idx8 ✅ ／ idx9 ❌ ／ idx10 ⚠️ ／ idx11–15 ❌ ／ idx16–18 ✅ |
| 原始输入 | 指纹**未改动** ✓（130 文件 / 394,140,543 字节，跑批前后一致） |
| 分布留档 | `docs/12_v0.3.0_真实跑批分布_0916.json` |

### 批次 3：打包与冻结态冒烟（T14）

`build_exe.ps1 -Zip`（第一步强制走 `tools/check_env.py` 环境体检）：

| 项 | 结果 |
|---|---|
| 环境体检（打包机） | 通过（19 项关键依赖版本核对 + 23 项关键模块 import） |
| onedir 产物 | `dist\报关申报要素校验工具\`（`报关申报要素校验工具.exe` 10.37 MB + `_internal\`） |
| 分发包 | `dist\报关申报要素校验工具_v0.3.0_0916.zip`（**157,657,123 字节 / 150.4 MB**） |
| 内置规则 | `_internal\rules\` **8/8 YAML** 收全 ✓ |
| 冻结态 `--self-test` | **exit 0**，322 ms，输出「自检通过：规则加载 OK / 13 列 OK / 口径字符串 OK」 |
| 冻结态 `--check-env` | **exit 0**，648 ms；19 项依赖 OK、23 项 import OK、空壳包检测按设计**声明不适用** |
| 运行目录锚点 | 冻结态自动在 exe 同级创建 `报关申报要素校验\{result,logs}` ✓（`app_base_dir()` 生效） |
| 版本号 | `main.APP_VERSION` / `pyproject.toml` / `build_exe.ps1` 统一为 **0.3.0** |

> ⚠️ 仍**未做**：干净 Win10 1809+ 真机**双击验收**（需现场机器，沿用 v0.2.0 遗留项）。

### 批次 3：提交与推送（T15）

| 项 | 结果 |
|---|---|
| 已推送提交 | `dfe00d5` → **`4ad328b`**（`main`） |
| 提交规模 | **26 文件 / +5125 −570** |
| 新增文件 | `core/token_matcher.py`／`ui/widgets/ocr_text_dialog.py`／`tools/run_sample.py`／`tools/eval_v03.py`／`tests/test_token_matcher.py`／`docs/10`／`docs/11`／`docs/12` |
| 远端校验 | `git ls-remote` 远端 `main` = `4ad328b9d1730f0e7c70bf8a5ef0e364241bcf6d`，与本地 HEAD **逐字符相同** |
| 双向差异 | `rev-list --left-right --count origin/main...HEAD` → **`0 0`** |
| 忽略规则 | 新增 `build_log_*.txt` / `run_log_*.txt`（构建临时日志不入库，**未删除磁盘文件**） |
| 未入库（按设计） | `dist/`（含 v0.2.0 与 v0.3.0 分发包）、`原始输入/`、运行期 `报关申报要素校验/` |

### v0.3.0 遗留（转 v0.4 候选）

| # | 事项 | 状态 |
|---|---|---|
| 1 | 干净 Win10 1809+ 真机双击验收 | **未闭环**（唯一未闭环交付验收项，需现场） |
| 2 | 「整机品牌 → ❌」与「命中即合格」交叉场景（真实样本 idx1） | **待裁决** |
| 3 | 散行卡/链路表 深色→浅色、工作台默认列全部记录 | **待用户确认**（属交互/视觉变更） |
| 4 | KV 反哺判定（`Brand:XXX` 能解析却仍判 ⚠️） | **待裁决** |
| 5 | `intra_op_num_threads` 移出 `params_fingerprint`（不影响 OCR 输出却致全量缓存失效） | 建议 |
| 6 | 票号推断与 Excel 出货通知书号交叉校验 | 建议 |
| 7 | `投票来源图号` UI 近似推断已删除，引擎回吐已就位 | **已闭环** ✓ |

